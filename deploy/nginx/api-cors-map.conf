# Browser clients (Infinite Canvas, web UIs) call OpenAI-compatible APIs.
# Echo the request Origin; omit credentials so any webpage can use API keys.
map $http_origin $api_cors_origin {
    default $http_origin;
    ""      "*";
    "null"  "*";
}
