# 0.1.350 发布与部署记录

工单页在没有 `crypto.randomUUID` 的浏览器里一打开就白屏。客户端改为自己生成 UUID，幂等键格式不变。无数据库迁移，无价格、倍率、支付或分组变更。`DATABASE_MIGRATION_MODE=validate`，预扣保持关闭。

349 留着当备份，进程不关。

## 发布

- 修复提交 `6da33468a`，版本提交 `2edf65698`。二进制 SHA256：`b9aec8d1e603564f09d46e23ee75ea7e0ee6aa9903f6aad2479154c9c54d8905`。
- 前端入口：`/assets/index-9Ju_0RXt.js`、`/assets/index-DR6UDIe9.css`。

## 滚动上线

- 候选：350 / 内网 8210 / pprof 6104。稳定回退：349 / 8140。8142 被旧机出站连接占过，没有用。
- 1% 大约 5 分钟，350 有真实 200，无平台 panic，NRestarts=0。
- 两台切到 350 全量。349 作为 nginx backup。

| 节点 | 业务流量比例 | 350 PID | 保留的 349 PID | 启动时 NRestarts |
| --- | --- | --- | --- | --- |
| ns575199 | 40% | 2618012 | 2117494 | 0 |
| ns572368 | 60% | 3042292 | 3003595 | 0 |

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-350-20260924/rollout350_guard.py rollback`。新机命令前加 `sudo -n`。只切回 349 流量，不停应用、不删旧资源。
