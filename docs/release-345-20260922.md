# 0.1.345 发布与部署记录

创建和更新 OpenAI 账号时，类型 `cloudflare` 可以通过校验。之前保存 Cloudflare 账号会返回 400：`CreateAccountRequest.Type` 的 `oneof` 白名单里没有它。无数据库迁移，无价格、倍率、支付或分组开关变更。前端仍是 `index-CvqdRKgG.js`。`DATABASE_MIGRATION_MODE=validate`，`GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`，预扣保持关闭。344/343/342 进程全程不关。

Cloudflare 的模型搜索接口不返回 `typesafe/jev`。这个号的搜索能搜到 `llama`，搜 `typesafe` 是 0 条。直接调用 `/ai/run` 时，Cloudflare 返回余额不足，不是找不到模型。同步按钮仍然不会自动出现它。要卖这个模型，先在 Cloudflare 给 Workers AI 充值，再在映射里手填模型名。

## 发布

- 标签 [v0.1.345](https://github.com/kiss-kedaya/sub2api/releases/tag/v0.1.345)，提交 `e48250dfa`。
- 两机生产二进制 SHA256：`f8971b7d5dcf8fe427b303d812d3fa6cf72b19bcce719e1920ea06fa9d0dd535`。
- 前端入口不变：`/assets/index-CvqdRKgG.js`、`/assets/index-ihyzV4JN.css`。

## 滚动上线

- 候选：345 / 内网 8132 / pprof 6094。稳定回退：344 / 8130。343 留在 8128，不接主流量。
- 流量比例保持旧机 40%、新机 60%。1% 灰度：344 权重 3960/5940，345 权重 40/60，backup 8128。
- 切 1% 前已停 344 守卫。345 守卫继续每 10 秒检查。
- 1% 约 8 分钟。切全量前旧机 345 业务成功 138 次，新机 50 次；无平台 panic，NRestarts=0。
- 旧机 UTC 15:44:45、新机 15:44:46 切至 345 全量。344 作为 nginx backup，进程不关。

| 节点 | 业务流量比例 | 345 PID | 保留的 344 PID | 343 PID | 启动时 NRestarts |
| --- | --- | --- | --- | --- | --- |
| ns575199 | 40% | 189801 | 122584 | 3690735 | 0 / 0 / 0 |
| ns572368 | 60% | 2848829 | 2843041 | 2837062 | 0 / 0 / 0 |

## 验收

- 两台 origin `/readyz` 为 ready。两边公网 Host 探活 ready。
- 守护 `watching / percent=100 / armed=true`。纯上游 502/503/429 不回退。
- 344 守卫保持停止。345 守卫每 10 秒检查，失败只切回 344/8130，不停进程。
- 后台刷新后，OpenAI 选 Cloudflare，填 Account ID 和 API Token，保存不再报类型校验失败。

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-345-20260922/rollout345_guard.py rollback`。新机命令前加 `sudo -n`。只切回 344 流量，不停应用、不删旧资源。
