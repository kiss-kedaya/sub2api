# 0.1.348 发布与部署记录

模型广场补上模型列表里有、渠道没定价的模型。渠道里已经有的不重复。没渠道价的用官方目录价。白名单关掉的不进广场。无数据库迁移，无价格、倍率、支付或分组开关变更。前端入口仍是 `index-CvqdRKgG.js`。`DATABASE_MIGRATION_MODE=validate`，`GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`，预扣保持关闭。

347 留着当备份，进程不关。345 仍在跑，但不在 nginx 里。

## 发布

- 代码提交 `f74a5809a`。版本文件改为 `0.1.348` 后编译。二进制 SHA256：`03a246f093ca304d6e9781176779d444bbb28a804ddd9ac4ae249eb78a378f86`。
- 前端入口不变：`/assets/index-CvqdRKgG.js`、`/assets/index-ihyzV4JN.css`。
- 候选端口用 8128。旧机 8138 被本机连 347 的临时端口占着，绑不上。

## 滚动上线

- 候选：348 / 内网 8128 / pprof 6100。稳定回退：347 / 8136。
- 切 1% 前已停 347 守卫。1% 大约 4 分钟，348 有真实 200，无平台 panic，NRestarts=0。
- 两台切到 348 全量。347 作为 nginx backup。

| 节点 | 业务流量比例 | 348 PID | 保留的 347 PID | 启动时 NRestarts |
| --- | --- | --- | --- | --- |
| ns575199 | 40% | 1337076 | 998614 | 0 |
| ns572368 | 60% | 2936677 | 2910606 | 0 |

## 验收

- 两台 348 `/readyz` 为 ready。旧机公网 Host 探活 ready。切全量后 8128 上有活动连接。
- 守护 `watching / percent=100 / armed=true`。纯上游 502/503/429 不回退。

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-348-20260923/rollout348_guard.py rollback`。新机命令前加 `sudo -n`。只切回 347 流量，不停应用、不删旧资源。
