# 0.1.353 发布与部署记录

北京时间 2026-09-25 完成，用户当次明确授权并行验收、commit、打版上线。正式仓为 `F:/GO/sub2-official-work`，`main` 功能提交 `498a2882a52e320f841016f326796229f3830e3b`，已推送 `kiss-kedaya/sub2api`。

## 用户可见变化

- 移除访问白名单页面、侧栏入口和用户 API；旧 `/api/v1/user/cf-allowlist` 返回 404。
- 支付宝、微信内嵌收银台加载超时后提示新窗口打开或更换网络，不移除 iframe；迟到加载成功后撤销提示。
- 管理员工单展示今日 Token、实际消费和近14天充值。统计失败显示 `—`，不伪装成零；两项可用状态独立返回。
- 近14天充值指14个自然日的已支付余额订单 `amount`，包含待到账订单，不含卡密与管理员加款，不使用 `pay_amount`。页面提示明确口径。
- 单账号、批量账号复用测模型结果表，支持筛选、勾选、测全部、停止和查看结果/耗时；取消与重跑隔离，移动端表格可横向滚动。

没有修改生产价格、倍率、余额、表结构或模型映射。迁移模式保持 `validate`，预扣保持关闭。

## 验收

- 前端关键回归 52 文件 / 535 测试通过。新功能定向107项、locale与工单最终18项通过，存在重叠，不累计为总数。lint、完整前端构建、Linux amd64 embed 发布构建通过。
- 后端 repository/service/routes 定向测试通过，使用本地隔离 PostgreSQL，不在生产数据库试写。统计查询各750ms独立预算；30万明细、24万汇总、20万订单下走索引，无顺序全表扫，单查询0.024～0.261ms。
- 发布包模拟21项通过。手机/桌面测模型、收银台5秒超时及迟到恢复已在本地浏览器验收。
- 全量 Go 测试不是全绿：`TestForwardGrokResponsesRetriesInvalidEncryptedContentOnce`、`TestSmartRouteLazyCatalogPreservesSelectionOrder` 在隔离352基线复现；integration缺少 `rateLimitAccountRepoStub` 同样在352复现。Windows三项测试缺 `sh`，urlvalidator单测执行偶发被拒、integration对应包通过，repository integration环境超时。未夹带修复无关旧问题。
- 生产两机真实工单接口200，列表19条，抽查工单的用量和充值可用状态均true；已移除API均404。
- 公网Chrome只读验收通过：工单三项统计实际显示，测模型新面板实际打开；checkout-info 200，health/readyz 200，新JS/CSS 200，页面异常和失败静态资源均为0。没有触发真实充值或发起收费模型测试。
- APIRequestContext直连曾遇到403/连接中断，改为真实浏览器同源请求后通过；不把测试客户端网络失败当成生产修复。收银台异常分支是本地验证，未宣称真实支付到账闭环已跑。

## 构建标识

- 二进制 `sub2api-0.1.353` SHA256：`beefd7f8fd9679f9fa2050ae5a462b6bd23be70bcc4e44799316f26f77183c43`。
- `ui-assets-353.tgz` SHA256：`b70eed080dbe4eaed0fc6d3869ac977bb6109d84f5fbd0cdc9f9af8ad68304ca`。
- `index.html` SHA256：`f543f57435a80bd8f6ead1a438d12f25357db010cc5452446ed86b35004daaf3`。
- 入口 `/assets/index-CBTXk_fH.js`，样式 `/assets/index-en0NI3_A.css`。新机 `/opt/sub2api/ui/index.html` 已切换，旧机继续使用二进制嵌入HTML。

## 双机滚动结果

- 353内网端口8216，pprof仅127.0.0.1:6110；352内网8214为回退版本，350内网8210进程仍保留。
- 本次开工实测为352主流量、350 backup，和旧352文档最初记载的351 backup不同，以现场为准。
- 两机预置全部新assets，备份状态和配置后，明确disable/disarm 352守卫；353独立守卫接管。不重复初始化旧状态。
- 1%从UTC 2026-09-24 18:40:53/54开始。晋级前旧机观察456秒、38次健康检查、累计106条真实2xx；新机461秒、43次健康检查、累计52条真实2xx。最近检查分别12秒、10秒，均通过门槛。未造流量补数。
- 旧机UTC18:48:53、新机18:48:59切100%，即北京时间2026-09-25 02:48:53/59。两处nginx均为353主流量旧40%/新60%，352 backup同样40/60。
- UTC18:53:15/22，353守卫仍armed且100%；全量后健康检查23/25次，最近180秒真实200分别4635/1939。日志仍有少量业务502/503，不宣称所有业务请求成功；未见平台panic，健康探测均200，未触发回退。
- 两机353、352、350应用均active、NRestarts=0；旧进程和长请求保留。内网临时传包端口18953已关闭。

| 节点 | 353 PID | 保留352 PID | 保留350 PID | 主流量权重 |
| --- | --- | --- | --- | --- |
| ns575199 | 3407087 | 2929205 | 2618012 | 40% |
| ns572368 | 3102765 | 3065502 | 3042292 | 60% |

## 回退入口

状态和备份目录：`/opt/sub2api/rollout-353-20260925`。两机分别执行其中 `deploy353.py rollback`，只切回健康352和新机旧静态入口，不停任何应用、不删assets。

```powershell
ssh -i C:/Users/34438/.ssh/id_ed25519 -o BatchMode=yes root@51.222.42.218 python3 -B /opt/sub2api/rollout-353-20260925/deploy353.py rollback
ssh -i C:/Users/34438/.ssh/id_ed25519 -o BatchMode=yes debian@51.161.119.83 sudo -n python3 -B /opt/sub2api/rollout-353-20260925/deploy353.py rollback
```

回退后不要直接重新启用352旧timer；它的旧配置指纹与当前回退配置不同。守卫按平台故障回退，不因孤立上游429/502/503自动切流。

本地验收和基线证据在 `.artifacts/handoff-20260925-0105`，发布包在 `.artifacts/release-353`；这些目录不提交。原有 `.learnings/LEARNINGS.md`、`docs/ops-learnings-20260922.md` 的未提交改动保留，不夹带进入本次提交。
