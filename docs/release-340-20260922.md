# 0.1.340 发布与部署记录

上游 `https://senseaudio.cn` 一类账号返回 HTTP 400、`ref_code=400901` / `计费账户已被冻结` 时，号仍留在调度池，请求反复打坏号。340 命中该冻结错误后把账号标 `error` 且不可调度，并换下一号。普通参数 400 不禁用。后台点测试打到同一错误也会停调度。

无数据库迁移，无价格、倍率、支付或分组开关变更。`DATABASE_MIGRATION_MODE=validate`，`GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`，预扣保持关闭。旧机 `DATA_DIR=/opt/sub2api/data-316`。339/338/336 进程全程不关。

## 发布

- 标签 [v0.1.340](https://github.com/kiss-kedaya/sub2api/releases/tag/v0.1.340)，提交 `226926649f4abd69b17dc2888b6fcbe429cc6304`。
- 主提交 Security `35690159412` 成功；CI `35690159409` 失败。标签 Security `35690162600`、Release `35690162565` 成功；标签 CI `35690162583` 失败。重复 Release `35690162065` 被并发策略取消。
- 失败测与 339 相同：`TestForwardGrokResponsesRetriesInvalidEncryptedContentOnce`、`TestSmartRouteLazyCatalogPreservesSelectionOrder/absent_models_are_skipped`。未作为回退条件。
- GitHub Linux amd64 归档 SHA256：`c2f4013ffd9343732d1d0545a337f55f5111a2905b467e3c46f5dac1fb5083a1`。
- 两机生产二进制 SHA256：`8dec8c67a1614396bcfd23a68ea08f0b7cb3a380c84701df5b1ca4e55927fbcd`。
- 前端无改动，仍为 338/339 入口 `/assets/index-DlKpmTe8.js`、`/assets/index-BgSWdc1U.css`。

## 滚动上线

- 候选：340 / 内网 8122 / pprof 6084。稳定回退：339 / 8120 / pprof 6082。338 留在 8100，336 留在 8096，均不接主流量。
- 流量比例保持旧机 40%、新机 60%。1% 灰度：339 权重 3960/5940，340 权重 40/60，backup 8100。
- 新机昨天残留的未剥离 173MB `0.1.340` hotfix 已替换为本包；unit/env 从 8120/6082/debug 改为 8122/6084，未开 debug 日志。
- 旧机临时端口范围仍为 1024-65535。向 `/etc/sysctl.d/99-sub2api-rollout-ports.conf` 追加 6082、6084、8120、8122。新机范围从 32768 起，未调整。
- 切 1% 前先 disarm 339 守卫，避免改 nginx 时被切回 338。
- 1% 灰度约 8 分钟。切全量前旧入口 340 业务成功 65 次，新入口 53 次；无 `openai.responses_panic_recovered`，无新增平台 panic，NRestarts=0。
- 旧机 UTC 05:37:13、新机 05:37:12 切至 340 全量。339 作为 nginx backup，进程不关。338/336 进程不关。

| 节点 | 业务流量比例 | 340 PID | 保留的 339 PID | 338 PID | 启动时 NRestarts |
| --- | --- | --- | --- | --- | --- |
| ns575199 | 40% | 1570899 | 1532627 | 584902 | 0 / 0 |
| ns572368 | 60% | 2771764 | 2766254 | 2677876 | 0 / 0 |

## 验收

- 两台 origin 和公网 `https://kedaya.ai` `/readyz` 为 ready，首页 200。
- 守护 `watching / percent=100 / armed=true`。纯上游 502/503/429 不回退。
- 339 守卫已 `disarmed`。340 守卫每 10 秒检查，失败只切回 339/8120，不停进程。

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-340-20260922/rollout340_guard.py rollback`，只切回 339 流量，不停应用、不删旧资源。
