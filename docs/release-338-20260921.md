# 0.1.338 发布与部署记录

337 因 Grok `/v1/responses` 转 Chat 时多个终端工具调用扩容拷贝 `strings.Builder` 触发 `openai.responses_panic_recovered`，已回退 336。338 修复该 panic，并让智能路由在选号前应用官方 `ClaudeCodeOnly -> FallbackGroupID`，计费用实际生效组。首页提供商改为官方 `PlatformIcon`。

无数据库迁移，无价格、倍率、支付或分组开关变更。`DATABASE_MIGRATION_MODE=validate`，`GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`。337 及其他旧进程保留，不截断已连接流量。回退目标是 336/8096，不是 337。

## 发布

- 标签 [v0.1.338](https://github.com/kiss-kedaya/sub2api/releases/tag/v0.1.338)，提交 `8f19d7792e02877675df4ffefe456ef43a48f3d6`。
- 主提交 CI `35602897603`、Security `35602897525`；标签 CI `35602923889`、Security `35602923923`；Release `35602923908` 均成功。重复 Release `35602923296` 被并发策略取消。
- Linux 归档 SHA256：`918642957b919d80085c9f3e4df80ae25ffe356c12699d4e05eeef0dc553fa7b`。
- 两机二进制 SHA256：`ec9d420308a18d140028ead215cf55f9acf4ab43147303ec8637f9ea1274ccc5`。
- 前端入口：`/assets/index-DlKpmTe8.js`、`/assets/index-BgSWdc1U.css`。入口 SHA256：`37a913fe43652e39c0e836a7f79a6471fab879743b58562b42be58ef030e1636`。

## 滚动上线

- 候选：338 / 内网 8100 / pprof 6080。稳定回退：336 / 8096 / pprof 6074。337 留在 8098 但不接流。
- 流量比例保持旧机 40%、新机 60%。1% 灰度：336 权重 3960/5940，338 权重 40/60。
- 旧机临时端口范围仍为 1024-65535。备份 `/etc/sysctl.d/99-sub2api-rollout-ports.conf` 后，仅向保留集合加入 6080、8100；运行值从 `6079,6081` / `8095-8099,8101` 变为 `6079-6081` / `8095-8101`。未改临时端口范围。新机范围从 32768 起，未调整。
- 旧机备份 `/root/sub2api-338-backup-20260921T131503Z`，新机备份 `/root/sub2api-338-backup-20260921T131503Z`。
- 1% 灰度约 9 分钟。切全量前旧入口 338 业务成功 163 次，新入口 71 次，均超过 50 次；无新增重启或 `openai.responses_panic_recovered`。
- 旧机 UTC 14:03:55、新机 14:03:54 切至 338 全量。336 作为 nginx backup。337 进程未停止。

| 节点 | 业务流量比例 | 338 PID | 保留的 336 PID | 启动时 NRestarts |
| --- | --- | --- | --- | --- |
| ns575199 | 40% | 584902 | 3184254 | 0 / 0 |
| ns572368 | 60% | 2677876 | 2578466 | 0 / 0 |

## 验收

- 两台 origin 和公网 `https://kedaya.ai` 返回 338 首页资源；`/readyz` 为 ready。
- 338 内嵌页与静态入口均引用 `index-DlKpmTe8.js`。
- 守护 `watching / percent=100 / armed=true`。纯上游 502/503/429 不回退。
- Claude Code `This group does not allow /v1/messages dispatch` 是入口组 `AllowMessagesDispatch=false`，不是本次调度修复能单独解开的开关。

回退入口：对应服务器执行 `sudo -n python3 /opt/sub2api/rollout-338-20260921/rollout338_guard.py rollback`，只切回 336 流量和静态入口，不停应用、不删旧资源。
