# 2026-09-15 热点与缓存专项审查

## 范围与证据

- 正式仓库 `F:\GO\sub2-official-work`。303基线 `498f5333e32d12bc509770b38ae0d93f8b212d2c`，重点复核312至315的缓存转换、SSE终止、取消排水与非流式错误处理。
- 两个独立工作树分别审查缓存与资源生命周期，补丁均由主线复核合入；子代理没有连接生产。此次不是全仓库所有功能的完整安全审计。
- 13:46在315接40%流量时采集20秒CPU、二进制heap与聚合goroutine，不采集请求正文，不公开上游凭据。诊断产物位于本地 `.codex/tmp/release-audit-0915/315-audit-current-*`。
- CPU样本65.57CPU秒：GJSON parseSquash约22.4%、parseString约8.2%；工具schema处理累计11.36%，Lite parallel字段处理7.63%。存活堆2.15GiB，请求体1.41GiB、input重写约0.28GiB。高RSS与在途请求相关，不能仅凭这些样本认定完全无泄露。

## 确证问题与修复

| 用户侧触发 | 修前行为 | 本轮修复与版本归因 |
| --- | --- | --- |
| Chat上游只回传兼容缓存字段，转换到Responses/Messages | 缓存128变成0，下游用量与本地可能不同 | `e0cbce18e`复用Responses缓存优先级；303和315均能复现，非最近新增 |
| 缓存写入字段分散在两种details，标准值明确为0/null | 多层序列化后低优先级旧值64重新出现 | 同提交统一缓存别名及显式零值；三次跨协议往返验证 |
| 上游发送response.cancelled/canceled及usage | 网关计费用量存在，下游终止usage丢失 | 两个转换器补取消终止事件；303已存在 |
| 首次正文或keepalive写给客户端失败 | 误分类为可切号错误，重试且丢已产生usage | `7e2704393`区分客户端写失败与本地暂存文件故障，前者按原预算排水 |
| 响应头返回后客户端取消，非流式或错误响应体卡住 | header保护已结束，ReadAll可能长期阻塞 | 同提交给取消后的buffered读取加现有排水预算，默认180秒；在线请求不加超时 |
| 已脱离取消的执行上下文处理非法非流式响应 | 原客户端已取消却仍生成切号错误 | 同提交复核原始HTTP请求上下文，不再重复上游请求 |
| 两个请求都看到缓存未命中，前一个查询已经填充缓存 | 晚到的请求仍重复查询数据库，既有CI偶发失败 | `420217575`在singleflight leader内再次查缓存；确定性测试修前失败、修后通过 |

315的`aaf4dcfe5`只补了Responses输入的缓存别名，没有覆盖Chat输入、自带显式0的多层往返及取消终止；这次针对这些剩余缺口补齐。不能把这些构造用例等同于用户反馈的具体请求，因为仍未拿到该笔请求ID。

## 有证据的性能优化

`50488e1ba`只修改OpenAI工具schema处理和Lite无变化分支：

- 两次工具参数解析合为一次，保留原始字节偏移一次应用；超大、解析限制、重叠补丁等情况回退原顺序路径。Grok平台保持原路径。
- 1MiB同时需要类型与pattern修复：约8.4ms降至4.2ms，分配2.11MB降至1.06MB。
- Lite的parallel_tool_calls已经false时，严格校验JSON、按解码后的重复键最后值判断后原样返回；不解码大型正文map。
- 1MiB Lite无变化请求：约4.1--4.3ms降至2.0ms，约5.24MB/31次分配降至基准中0次分配。
- 这些是本机局部基准，不代表端到端延迟或整站CPU降低相同比例。融合失败的罕见路径会额外尝试一次解析；128MiB边界和百万编辑上限未做动态压力测试。

## 防漏缓存验证

- 明确区分缓存命中计数、prompt_cache_key路由标识、cache_control声明、encrypted_content/签名，不把它们混成一个字段。
- 新增Chat/Responses输入、三协议转换、流式/非流式、显式0/null/负数、字段冲突、三次序列化往返、取消终止、原始标签字节、Grok双层包装的回归。
- 对比原顺序实现的输出字节、错误和changed标志；工具schema模糊测试35秒，共115806次输入，无差异。模糊输入上限64KiB。
- 在线315真实复测：Chat缓存2048对应usage181238271；Messages缓存128对应181238900；Responses缓存128对应181239798，均HTTP200，输出与账单一致。这验证当前正常路径；新构造边界修复尚未上线。
- Chat->Responses部分audio/reasoning细分明细丢失是303遗留问题，本轮未扩展修复。新Chat usage解码增加少量解析开销，未发现其为生产热点，优先保证字段一致。

## 验证与生效状态

- 最终源码 `e0cbce18e4f76b5b2228bb5ebaf7887d1e9f2beb` 已推送main。
- 本地完整后端unit：20413项测试，57个包，0失败。联合race覆盖协议、取消、header交接、默认180秒、100次并发Close、重复查库等，通过。
- Linux CI `34935615513` 全绿：单元、集成、golangci-lint、前端、部署脚本；安全检查 `34935615474` 成功。
- 本轮未创建316tag、未发布或部署新二进制。新缓存/释放修复和性能优化尚未对线上生效；不能把本地测试通过报告为生产已修复。

## 生产容量与运维复查

- 14:02:08可用内存低于8GiB，原守护自动从60/40退回312全量。
- 14:11:54，315已退流约十分钟，312 RSS约38GiB，整机耗尽；内核再次杀312 PID2015025。systemd拉起为PID2022837，NRestarts=2，14:12:53公网health/readyz恢复200。这次旧连接受影响，不能声称无损或旧实例始终未中断。
- 证据说明内存压力不是只发生在315。旧312全量后也很快积压，按整机内存不足自动将所有请求压回旧实例会进一步集中负载，不能视为内存修复。
- 14:18恢复用户指定旧312 60%/新315 40%，独立目录 `/opt/sub2api/rollout-0.1.315-balanced-after-oom` 保留备份与状态；当前upstream SHA256 `818fbfec9b5373cdc1b5c19292feade477278dfea95703f1d90ffad4bf3d9809`。
- `sub2api-315-balanced-watch`临时运行60分钟，每5秒检查公网/候选健康、旧PID与配置。公网异常立即回退；内存不足改为MEMORY_PRESSURE记录，避免仅因整机内存再次集中到312。该守护不是永久监控，不自动全量。
- 所有生产数据库、业务配置和凭据未改；应用未被人为停止。整体容量及长时间内存风险仍未解决，下一次发布需结合以上修复和实际并发再验证，不能承诺不再OOM。

## 14:31最新状态

- 14:23:06内核再次OOM杀312 PID2022837，systemd拉起PID2023433；14:25:17再次OOM后恢复PID2023530，NRestarts=4。315、313、两份303的PID未变。不能只报告首次OOM，不能声称这些旧连接未受影响。
- 根分区ext4、空闲约417GB。为避免继续直接杀进程，14:25--14:26创建并启用32GiB临时交换文件 `/var/swap-sub2api-emergency-20260915`，权限0600。未修改fstab、sysctl、数据库或应用配置；重启不会自动再次启用。内存不足时不能直接swapoff，应在充足内存下另行排空后移除。
- 临时交换空间只吸收突发容量，不能修复泄露或无限等待，且可能增加延迟。14:30交换用量约10GiB，公网一度超时，不能称为稳定验收。
- 14:27:49恢复用户60/40，目录 `/opt/sub2api/rollout-0.1.315-balanced-swap`；14:30:58公网502触发立即回退。该监控已退出，当前没有正在运行的这轮自动灰度守护。
- 14:31:15公网health/readyz回到200，当前312主流量，315在线待命；upstream SHA256 `3de77d9e0290bd552db410d37b8963fba9f19960909cc34b65c4a5502d140738`。可用内存约1.4GiB、交换用量约9.3GiB，仍有严重容量风险。
- 本轮新源码仍未打版、未上线，不能将测试通过当作生产问题已根治。后续必须在容量可承受的条件下验证新的释放补丁；反复重放切流/回退不能替代根因治理。
