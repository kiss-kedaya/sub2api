# 0.1.318 发布、流式与灰度核验

日期：2026-09-15，时间为北京时间。正式仓库为 `F:/GO/sub2-official-work`。

## 发布对象

- GitHub Release：`v0.1.318`，标签提交 `2501dcb0a80c521672e87cf30620d4879bb29dd8`。
- Release、CI、安全扫描均成功；另一个重复 Release 工作流被取消，不能误判为实际发布失败。
- Linux amd64 压缩包 SHA256：`a611169ff6ab41f67aa5c419b8ce0deff36622912ced6677d4c69b8c0c3e9972`，继承上一轮下载核验记录。
- 两机本轮实际二进制 SHA256 均为 `9602ac2a092e44f4e1e30c36c0b3bc5c547f75de641128610bdb75cf4a13d9fb`；内置版本、提交号与标签相符。
- 正式 main 快进到 `895b044dd`，仅同步 VERSION；未移动标签或重复创建版本。

## 本轮验证的行为

318 修正 Responses 首次完整输出在阻塞 flush 后才计时的问题，OpenAI 转 Chat/Messages
复用既有首输出分类，并在只有终止快照含正文时补发正文。已有正常正文增量不重播。
本轮重新审查 316 到 318 的实现差异，没有修改缓存解析、计费、选号或数据库结构。

真实请求要求生成 20 行正文，逐行读取 SSE 并记录不同时间窗口；使用短 OK 回答
不能证明流式正常。请求读取设置 identity，避免把客户端解压或缓冲算成服务端问题。

| 入口与模型 | HTTP | 首正文 | 总耗时 | 正文片段 | 下游缓存 / 账单缓存 |
| --- | --- | --- | --- | --- | --- |
| 新机 318 Grok Chat | 200 | 28.892s | 33.112s | 364 | 128 / 128 |
| 旧机 318 Grok Messages | 200 | 28.974s | 34.714s | 378 | 128 / 128 |
| 新机 318 GPT 5.6 Chat | 200 | 12.498s | 29.853s | 272 | 1280 / 1280 |
| 旧机 318 GPT 6 Responses | 200 | 6.386s | 11.584s | 248 | 0 / 0 |
| 全量后公网 Grok Chat | 200 | 39.546s | 46.437s | 338 | 2176 / 2176 |
| 全量后公网 GPT 6 Messages | 200 | 14.068s | 29.233s | 262 | 1664 / 1664 |

- 公网两笔分别分布在 26、95 个不同的 100ms 时间段，确认正文在终止前持续到达。
- 公网 Grok 账单：普通输入 7、缓存 2176、输出 2395；下游 Chat 的总输入为 2183，拆分一致。
- 公网 GPT 账单与下游 Messages：普通输入 1358、缓存 1664、输出 294，逐项一致。
- 对账使用响应头返回的 `X-Client-Request-ID`，查询 `client:<id>` 和 api_key_id 的联合索引。
  网关会生成自己的关联编号，不能假定发送的自定义 Header 值就是账单编号。
- 账单 `first_token_ms` 当前沿用 semantic 首输出口径，可能早于正文；这与客户端首正文时间是不同指标。
  上游只在结束时给出正文的样本仍只能按实际到达计时，不能伪造更早首字。
- 本地重新执行 apicompat 全包、`TestStreamTimingReview_` 服务回归和 `git diff --check`，均通过。

## 灰度过程与回退

- 接手时两机均为 316 95% / 318 5%，318 已运行近 3 小时，重启次数为 0。
- 22:26 从 5% 升到 20%，22:32 升到 50%，各完成至少 3 分钟连续健康检查。
- 22:36 两机入口切到 318 100%；新请求按旧物理机 40% / 新物理机 60% 分配。
- 两机 316 原 PID `3669340`、`2025587` 始终保留；318 PID `3886077`、`2029503` 始终不变。
- 全量配置用两机 316 作 backup；既有连接继续由旧 Nginx worker 和旧应用处理。仅执行 Nginx reload。
- 每阶段先确认实际配置与预期一致，再备份、原子替换、`nginx -t` 和 reload。
- 备份位于两机 `/opt/sub2api/rollout-318-final-20260915/phase-{20,50,100}/`。
- 本轮监控检查公网 health/readyz、本机公网入口、两机两版 readyz、PID 和内存。
  任一健康失败、PID 变化、318 RSS 超过 17GiB 或整机可用内存低于 8GiB时，入口恢复为两机 316。
- 增流前额外要求可用内存大于旧主实例 RSS 加 8GiB，给新旧请求重叠留余量。
- 备用 `10.254.0.1:8080` 到新机 312 的历史路径在增流前实际返回 200。
  312 本轮保留；没有把正在使用的回退配置指向已删除端口。
- 旧 Grok 会话的 303 定时任务最后执行于昨日，当前没有 Grok 进程。
  已备份其 resources_state.json 并只清空该 Scheduler.tasks；解析比较确认其他状态相同。

## 热点、边界与经验

- 20% 新机压缩 heap 样本存活约 443MiB，请求体约 250MiB（56.4%）。
- 全量初期存活堆约 2478MiB，请求体约 1384MiB（55.8%）；其次为请求转换副本和 JSON 序列化。
- 10 秒 CPU 样本为 6.08 CPU 秒，主要消耗在 GJSON 扫描、JSON 校验和压缩、字符串处理。
- 全量监控中 318 新机 RSS 约 3--5.4GiB，旧机约 1.7--3.7GiB，存在回落；316 RSS 随旧请求结束下降。
  短窗口没有证明长期泄漏根治。大请求体与转换副本仍是后续优化对象，不能直接用少量 1% 样本预测全量。
- 50% 时 CPU 抽样新机 11.6%、旧机 23.6%；旧机还承载 PostgreSQL/Redis，维持物理 40/60。
- 初始 GPT 小请求因分组要求至少 2000 输入 token 被拒绝，补足测试上下文后继续；未改用户限额。
- GPT 5.5 返回上游无可用渠道；另有一次 GPT 5.6 Responses 返回 HTTP 200 后携带 SSE error，
  不能把 HTTP 200 或 created 事件算作成功。成功验收必须有正文、正常终止、usage 和账单。
- 业务仍有上游 502/503、路由暂无账号、Gemini 模型不可用等错误。50% 对照未发现 318 错误率明显恶化。
  ops_error_logs 含上游重试尝试，数量不能直接当作最终客户端失败率；最终结果优先看入口 access log。
- Nginx 按完成时间记录请求；统计按完成时间减 request_time 筛选本阶段新请求，单列旧连接尾流。
- 本轮没有修改业务配置、数据库、sysctl、网络参数或提高限额；未因单次错误尝试修改渠道。
- 敏感请求正文、密钥、邮箱和上游内部地址不进入本报告。原始性能样本与监控日志保存在忽略目录 `.artifacts/`。

## 最终监控

- 22:36:20--22:41:20，两机各完成 300 秒健康守护，均退出 0，没有触发回退。
- 随后入口日志复核，本阶段新请求全部发往 318；316 只出现切换前请求完成的尾流。
  两入口日志内分别有 4514、4531 次 health/readyz 请求，全部 200。这不是去重后的全站请求数。
- 新入口记录 318 业务 7259 次，其中 95 个最终 5xx（约 1.31%）；旧入口记录 13042 次，
  其中 371 个最终 5xx（约 2.84%）。入口流量组成不同，不应相加视为独立唯一业务请求。
  这些真实业务失败仍存在，不用健康探针覆盖它们。
- 监控结束新机 318 RSS 5190920KiB、旧机 3478188KiB；整机可用分别为
  33801956KiB、36625000KiB。316 RSS 已分别回落至 872804KiB、663492KiB。
- 两机 316/318 仍 active，PID 均不变，NRestarts 均为 0；最近 30 分钟未观察到新 OOM。
- 公网 `/api/v1/settings/public` 返回版本 `0.1.318`。新请求立刻使用新版本，历史用量记录不回写。
- 本轮连续守护为有界发布监控，已经完成，不声称会话结束后仍有永久自动监控。
- 回退工具和阶段备份留在 `/opt/sub2api/rollout-318-final-20260915/`；仅切回流量，不停止两机应用。
