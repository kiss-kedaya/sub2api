# 0.1.342 发布与部署记录

登录/首页条款入口和公开法律页改成可达鸭主体的 OpenLux 风格文档站：隐私、服务条款、可接受使用、免责、退款、DPA、合规、支持地区。管理员空正文回退内置稿，必须点保存才入库。USDT 已上链原则上不退。无数据库迁移，无价格、倍率、支付或分组开关变更。`DATABASE_MIGRATION_MODE=validate`，`GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`，预扣保持关闭。旧机 `DATA_DIR=/opt/sub2api/data-316`。341/340 进程全程不关。

## 发布

- 标签待推送 `v0.1.342`。
- 无数据库迁移。

## 滚动上线

- 候选：342 / 内网 8126 / pprof 6088。稳定回退：341 / 8124。340 留在 8122 作 backup，不接主流量。
- 流量比例保持旧机 40%、新机 60%。1% 灰度：341 权重 3960/5940，342 权重 40/60，backup 8122。
- 切 1% 前先 disarm 341 守卫。
- 纯上游 502/503/429 不回退。

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-342-20260922/rollout342_guard.py rollback`，只切回 341 流量，不停应用、不删旧资源。
