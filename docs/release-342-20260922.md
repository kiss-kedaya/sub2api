# 0.1.342 发布与部署记录

登录/首页条款入口和公开法律页改成可达鸭主体的 OpenLux 风格文档站：隐私、服务条款、可接受使用、免责、退款、DPA、合规、支持地区。管理员空正文回退内置稿，必须点保存才入库。USDT 已上链原则上不退。无数据库迁移，无价格、倍率、支付或分组开关变更。`DATABASE_MIGRATION_MODE=validate`，`GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`，预扣保持关闭。旧机 `DATA_DIR=/opt/sub2api/data-316`。341/340 进程全程不关。

本次上线的是法律页，不是工作树里未提交的冻结号/黏连换号改动。

## 发布

- 标签 [v0.1.342](https://github.com/kiss-kedaya/sub2api/releases/tag/v0.1.342)，提交 `01b3e5211`。
- 主提交 Security 成功；CI 失败。标签 Security、Release 成功。
- 失败测与 339/340/341 同类，未作为回退条件。
- 两机生产二进制 SHA256：`d59cb7201c30a5ddbac9bcb1152df242d9f2f2fe8cc5279edc4a0a7079ba06cf`。
- 前端入口：`/assets/index-FxUsma-c.js`、`/assets/index-ihyzV4JN.css`、`/assets/catalog-jFD0jmGm.js`。入口 SHA256：`7a3f14b75c2590c24ebe22eec6c4a6e1397bd760d92698d15529430f2082ccc6`。

## 滚动上线

- 候选：342 / 内网 8126 / pprof 6088。稳定回退：341 / 8124。340 留在 8122 作 backup，不接主流量。
- 流量比例保持旧机 40%、新机 60%。1% 灰度：341 权重 3960/5940，342 权重 40/60，backup 8122。
- 切 1% 前先 disarm 341 守卫。
- 1% 灰度约 6 分钟。切全量前旧入口 342 业务成功 91 次，新入口 102 次；无 `openai.responses_panic_recovered`，无新增平台 panic，NRestarts=0。
- 旧机 UTC 11:50:31、新机 11:52:28 切至 342 全量。341 作为 nginx backup，进程不关。340 进程不关。

| 节点 | 业务流量比例 | 342 PID | 保留的 341 PID | 340 状态 | 启动时 NRestarts |
| --- | --- | --- | --- | --- | --- |
| ns575199 | 40% | 420572 | 1868831 | active | 0 / 0 |
| ns572368 | 60% | 2810106 | 2801237 | active | 0 / 0 |

## 验收

- 两台 origin `/readyz` 为 ready。新机公网 Host 探活 ready，`/legal/privacy` `/legal/disclaimer` 入口为 `index-FxUsma-c.js`，条款正文在 `catalog-jFD0jmGm.js`。
- 守护 `watching / percent=100 / armed=true`。纯上游 502/503/429 不回退。
- 341 守卫已 `disarmed`。342 守卫每 10 秒检查，失败只切回 341/8124，不停进程。
- 设置页正文要管理员点保存才入库；空正文前台已回退内置稿。

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-342-20260922/rollout342_guard.py rollback`，只切回 341 流量和静态入口，不停应用、不删旧资源。
