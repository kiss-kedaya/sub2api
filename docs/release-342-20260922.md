# 0.1.342 发布与部署记录

法律页已随 `01b3e5211` 进仓，但 342 还没打版上线。真正要随 342 出去的是冻结号自动禁用和黏连换号：池模式 DeepSeek 打到 `400901` / `计费账户已被冻结` 时标 `error` 且不可调度并换号；黏连号取不到或不可调度时清绑定，换号后黏到新号。经验全文见 `docs/ops-learnings-20260922.md`。

无数据库迁移，无价格、倍率、支付或分组开关变更。`DATABASE_MIGRATION_MODE=validate`，`GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`，预扣保持关闭。旧机 `DATA_DIR=/opt/sub2api/data-316`。341/340 进程全程不关。

## 发布

- 标签待推送 `v0.1.342`。
- 无数据库迁移。

## 用户侧变化

- 修前：冻结号继续接流，删号后会话还打旧号，用户看到 400 / 黏连错误。
- 修后：冻结号停调度并换号；死号黏连被清掉，下一发落到活号。
- 怎么验：组 36 不再打 `16213737342` / `16282198912`；新冻结号后台应变错误且不可调度。
- 生效：必须 342 二进制上线。现网仍是 341。

## 滚动上线

- 候选：342 / 内网 8126 / pprof 6088。稳定回退：341 / 8124。340 留在 8122 作 backup，不接主流量。
- 流量比例保持旧机 40%、新机 60%。1% 灰度：341 权重 3960/5940，342 权重 40/60，backup 8122。
- 切 1% 前先 disarm 341 守卫。
- 纯上游 502/503/429 不回退。

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-342-20260922/rollout342_guard.py rollback`，只切回 341 流量，不停应用、不删旧资源。
