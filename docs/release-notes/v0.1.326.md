## 修复

- 网关对不可用的 nginx 路由做 failover：不再重放已发出的请求，也不再因为单条通路失败就把账号停掉。

## 已知问题

- `grok-5.5` / `grok-5.6-sol` 在上游报 unknown provider / overload，属上游侧问题，本版未处理。

## 升级注意

- 无数据库迁移。
- Grok 响应头超时本版为 600 秒。
- 上线基线：旧机 0.1.326-grok600/8097，新机曾回退到 326/8095（内存余量低于阈值）。
- `DATABASE_MIGRATION_MODE=validate`，余额预扣关闭。
