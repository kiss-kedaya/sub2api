## 新功能

- iOS 风格控制台主题套到模型广场与遗留页。
- 账号工具菜单 teleport 后保持不透明。
- 智能路由候选改为按需读取，不再每次全量载入。

## 风险与已知问题

- 复核发现一个界面回归：管理员从内嵌广场点面包屑进用户 `/dashboard`，因按 `/admin/` 路径判定身份而跳错。已在本地预览期修复并补 4 项回归。**未改后端、价格、数据库。**
- 10,000 种候选组合覆盖未发现授权/顺序/计费组丢失。
- Live 未定价时拒绝消费的边界仍在。
