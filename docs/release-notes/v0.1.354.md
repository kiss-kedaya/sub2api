## 新功能

- 测模型弹窗重设计：账号信息只保留一处、统一按钮风格、小型状态标签 + 细进度条、「测试设置」可折叠保留填写内容、适配深色/手机/减少动态效果偏好。**不改测试请求、并发、取消、重跑和计费逻辑。**
- 包含 v0.1.353 的全部功能：
  - 移除用户侧访问白名单页面、侧栏入口和用户 API（旧 `/api/v1/user/cf-allowlist` 返回 404）。
  - 支付宝/微信内嵌收银台 5 秒超时提示新窗口/换网络，迟到加载后撤销提示。
  - 管理员工单展示今日 Token、实际消费、近 14 天充值；统计失败显示 `—` 不伪装成零。
  - 近 14 天充值口径 = 14 自然日已支付余额订单 `amount`（含待到账，不含卡密与管理员加款，不使用 `pay_amount`）。
  - 单账号与批量账号共用测模型结果表。

## 风险与已知问题（诚实记录）

- **全量 Go 测试不是全绿**：两条常红测试在隔离 352 基线上同样复现；integration 缺 `rateLimitAccountRepoStub` 也复现；Windows 三项因缺 `sh` 失败。**未夹带修复无关旧问题。**
- 收银台异常分支仅在本地验证，**未宣称真实支付到账闭环已跑通。**
- 无新增数据库迁移。

## 升级注意

- 自家双机已切到本版（8218）。
- **朋友机（Kegan）和小机当时未切换**：朋友机缺迁移 245、小机缺 37 项迁移，等数据库改动许可。**两处都不能用 schema 快照替代完整数据备份。**
- validate 模式下缺迁移会导致无法启动（朋友机实测）。
