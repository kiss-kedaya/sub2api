正式版本号 2.0.0，二开版本，不是上游官方发行版。

- 账号管理新增「上游余额」列，支持手动刷新与可见行自动刷新。
- 账号「更多」新增用量查询配置：通用 / NewAPI / Sub2API / 自定义模板，支持独立凭据、超时、刷新间隔、请求头、声明式 JSON 字段提取。
- 凭据不回显；查询限制响应大小、并发、冷却和目标地址，阻止内网访问与 DNS 重绑定。
- 在上游响应出口隐藏余额不足、额度耗尽与财务明细错误，未提交响应统一返回通用 502。
- 对照上游 v0.2.8 选择性手工移植 8 项修复，没有整树覆盖二开。
- 本版本相对 0.1.354 没有新增数据库迁移。
