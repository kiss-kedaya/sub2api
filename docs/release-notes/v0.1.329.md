## 新功能

- SIGNAL 设计上线到 dashboard、API Key、用量页。

## 修复

- 修前：328 的构建把 preview 代码打进生产配置，构建失败。
  修后：preview 拆到独立 `dev/vite.config.ts`，生产配置不再 import demo 代码；CI 增加真实生产构建 + preview 隔离测试。
- 修正 `/site-logo` 路由：新机曾返回 SPA HTML、旧机返回 JPEG。只加一条精确 proxy location，改前已备份。

## 风险与已知问题

- 后端相对 327 只改了 VERSION 和测试 fixture，结论是「未观测到新平台回归」，**不等于零业务错误**。
- 「匹配渠道状态/充值/相关用户页」的后续工作在 `feat/console-signal-user-pages` 分支，**不在本版**。
