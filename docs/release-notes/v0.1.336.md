## 修复

- 修前：335 的背景纹理走 `/images/*` 被网关保留路由吃掉，返回 404。
  修后：两张 WebP 从 public 移入源码 assets，由 Vite 内嵌进 CSS；生产构建强制检查深浅主题纹理均已内嵌。
- 修前：Chat→Responses 桥中指定函数 `tool_choice.function.name` 未展平、developer 消息被降级为 user、忽略 `output_item.done` 工具快照。
  修后：命名函数/allowed_tools 修复，保留 developer 角色，补齐终态工具快照并按 `call_id` 对齐。

## 风险与已知问题

- 后端相对 335 相同，仅前端移动纹理。
- **未取得真实用户的原始失败事件，不宣称解决了其全部 Agent 链路问题。**
- 仍存在上游拒绝/413、请求取消、缺终态、图片模型误投 Responses 等情况。

## 升级注意

- 无迁移；保留 334 作 backup；回退脚本 `rollout336_guard.py rollback`。
