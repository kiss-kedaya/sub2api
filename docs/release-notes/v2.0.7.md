- 首次引入独立渲染器：把模型产出的 HTML/SVG 渲染成四帧，用于动画与形变检查。
- 新增三层判定：结构层（文档完整性、SVG 闭合、外链依赖）、动画层（腿/轮/踏板/曲柄的旋转几何）、形变层（腿脚端点是否脱离踏板圆周）。
- 能识破「CSS 把腿挪走但脚留在原地」这类假动画。
- 调度门控：连续两次 degraded 才停号，明确 success 才恢复。
- 保守边界：含脚本、事件处理器、未知祖先变换、非线性 SMIL、暂停动画一律返回 unknown，不做靠名字猜的判定。
