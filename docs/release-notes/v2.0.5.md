## 降智检测改为复用定时检测结果

- 分组降智检测原先自己每 15 分钟另跑一轮探测，和分组里已有的定时检测计划重复。现改为聚合该组账号已有定时检测计划的结果。
- 迁移 248 删除 `group_quality_check_results` 表；`group_quality_check_settings` 保留为分组开关。
- 渠道状态卡片新增降智检测柱状图，与可用率柱对齐，跟随 90m / 24h / 7d / 30d 范围。
- 账号疑似降智临时停调度，下次检测正常后自动恢复。
- 修复渠道状态时间线的空槽位置：刷新后缺失的那一格出现在 PAST 侧，应在 NOW 侧。
