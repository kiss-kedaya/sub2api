## 修复

- 修前：保存 Cloudflare 账号返回 400，因为 `CreateAccountRequest.Type` 的 `oneof` 白名单里没有 `cloudflare`。
  修后：创建/更新通过校验。

## 风险与已知问题

- 模型搜索接口不返回 `typesafe/jev`；直接 `/ai/run` 上游返回余额不足。**要卖该模型需先给 Workers AI 充值，并在映射里手填模型名。**
