## 修复

- 修前：上游返回 `ref_code=400901` / 「计费账户已被冻结」的号仍留在池里，请求反复打坏号，用户直接看到 400。
  修后：命中后把账号置为 `status=error`、`schedulable=false` 并换号。普通参数 400 不禁用；后台测连接打到同错也停调度。

## 风险与已知问题（重要）

- **本版只覆盖非池模式。**
- **DeepSeek 是池模式**：`HandleUpstreamError` 对非 401 会直接 return，本版识别走不到。补丁当时还在工作树，未进本版。
