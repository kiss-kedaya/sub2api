## 修复

- 修前：本地并发打满时直接把 `Concurrency limit exceeded` 甩给用户；401 黏连死号反复打坏 key；DeepSeek `/v1/embeddings` 被路由门 404。
  修后：忙号排除后换号，全组忙才返回 `gateway_concurrency_limit`；401/凭证错误让黏连让位；embeddings 与 Responses 兼容平台共用放行集合。
- `/canvas` 同源 iframe 放开。

## 风险与已知问题（341 留下的边）

- **Grok 视频查状态/下载仍绑死创建号**：号忙最多等一会，超时仍可能把并发错误给用户；换号会找不到任务。
- **带 `previous_response_id` 的对话**粘号一忙就换号，**上一轮上下文会断**。
- 用户自己的并发上限满了仍返回 429，换号没用。
- **黏连死号清绑定这一截还在工作树，342 没带上。**
