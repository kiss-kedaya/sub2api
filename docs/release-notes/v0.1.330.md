## 新功能

- SIGNAL 控制台扩展到监控、充值、订单、订阅、兑换、个人资料、推广、IP 白名单页；管理端 overview/账号/分组/用户/用量布局更新。
- 充值中心可内嵌单独托管的支付页。

## 风险与已知问题

- 后端相对 329 只改了 VERSION；无迁移、无价格/币种/手续费变更。
- **观察到一条 Grok 候选首字 43723ms、总耗时 43807ms**。在同一账号 10 分钟窗口的 6842 条流里找到 40 条类似晚首字记录，判定症状早于本版、根因未解。**本版没有修复 Grok 缓冲问题。**
- 全量切换前后 5 分钟内组 9 各有 240/289 条 `Service temporarily unavailable` 路由 503，属平台侧而非上游，需单独排查。
- Python urllib 默认 UA 打公网 `/readyz` 被 Cloudflare 1010 拦，是既有出口限制，**不要因此放宽 WAF 或回退**。

## 升级注意

- 仅旧机补了 6081/8103 临时端口保留；新机范围已覆盖。
