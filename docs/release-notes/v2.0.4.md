## 新功能：分组降智检测

- 定时检测 runner 按分组开关，在该组最多探测 5 个可调度账号；降智的号暂停调度，恢复正常后自动放回。
- 管理端新增接口：`PUT/GET /admin/quality-check/groups[/:id[/results]]`。
- 渠道状态监控页新增「降智检测」设置卡片，可按分组逐项开关并查看状态徽章。
- 模型广场分组条目新增 `quality_status`（healthy / suspect），用户侧可见。

## 修复：禁用不安全的在线更新（对应 2026-09-26 事故）

- 修前：后台点「立即更新」会在**原位置替换正在运行的二进制**；新二进制起不来时，rollout 守卫把 nginx 改写到死端口，**整站下线**。
- 修后：`UPDATE_STRATEGY=disabled` 时后端直接拒绝 `PerformUpdate` / `Rollback` / `RollbackToVersion` 并返回明确错误；前端在禁止在线更新的部署下隐藏更新与回滚按钮并给出提示。
- 顺带修复账号表格横向溢出。

## 风险与已知问题

- 事故记录：真库未丢（旧机 Docker Postgres 全程健康，9730 活跃用户）；恢复靠起 `sub2api-2.0.0-canary`（8220）并改 nginx upstream 回 8220，**改前必须先 stop+disable `sub2api-200-rollout-guard.timer`**。
- 旧机上还存在 `a-sub2-*` 容器和 `upstream_a_sub2.conf`（另一个项目），改 kedaya_sub2 时不要指过去。

## 升级注意

- **新增迁移 `247_group_quality_check.sql`**（`group_quality_check_settings` + `group_quality_check_results`）。
