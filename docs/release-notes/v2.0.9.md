- 定时质检新增「按时间排序的四帧」校验：逐帧比对渲染结果，能抓到「单帧看着正常但动画时序错乱」的情况。
- 判定分三档：任一明确问题 → degraded；全部通过 → success；脚本/SMIL/未知变换/渲染失败/超限 → unknown。
- 停调度规则：连续两次 degraded 才停号；明确 success 才恢复；failed/unknown 打断连续计数且不动调度。
- 质检结果与请求成败分离：请求成功不等于质量合格，空正文不能绕过质检。
- 修复 runner 的 leader 锁竞态与质量暂停的缓存回收竞态。
- 随包附带独立渲染器 renderer/（Node.js 20.19+，强制 chromiumSandbox，拒绝 root 启动）。
