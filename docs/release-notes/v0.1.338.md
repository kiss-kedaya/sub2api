## 修复

- 修前：337 在 Grok `/v1/responses` 转 Chat 时，多终端工具扩容 `strings.Builder` 触发 `openai.responses_panic_recovered`，因此 337 被回退。
  修后：修复该 panic。
- 智能路由选号前应用官方 `ClaudeCodeOnly -> FallbackGroupID`，计费按实际生效组。

## 风险与已知问题

- 无迁移。
- Claude Code 报 `This group does not allow /v1/messages dispatch` 属于**入口组开关**问题，本版的调度修复解不开。

## 升级注意

- **回退目标是 336/8096，不是 337。**
