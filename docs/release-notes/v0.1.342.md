## 新功能

- 法律页上线：隐私、服务条款、可接受使用、免责、退款、DPA、合规、支持地区，OpenLux 风格文档站。

## 风险与已知问题

- 管理员留空正文时前台回退内置稿，**必须点保存才入库**。
- USDT 已上链的部分原则上不退。
- **本次上线的是法律页，不是工作树里未提交的冻结号/黏连换号改动。**
- 新机 nginx SPA 根是 `/opt/sub2api/ui`，只换二进制用户看到的还是旧 `index.html`，法律页看起来像没上线；旧机 HTML 走后端嵌入。

## 升级注意

- 无迁移。
