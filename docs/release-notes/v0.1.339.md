## 修复

- 修前：利润门在切组后仍吃**入口组**的阈值。组 43 阈值 0.18 否决了 DeepSeek 组 36 等未开门分组，表现为 **50ms 502** 或 `all candidates rejected by group profit control`，请求根本没出站。
  修后：切到实际调度组就重绑利润门；门为空则清掉入口残留。
- 未映射的 `grok-4.7` 落到同平台 Grok 号。
- Responses 上游强制 stream；入站 `/v1/responses` 不再预降级 Chat。
- Codex `local_shell_call` 回桥。

## 风险与已知问题

- **CI 从本版起有两条常红测试**（已知，不是回退条件）：
  - `TestForwardGrokResponsesRetriesInvalidEncryptedContentOnce`（会话 ID 改走缓存头）
  - `TestSmartRouteLazyCatalogPreservesSelectionOrder/absent_models_are_skipped`（未映射 GPT 按官方打同平台组）
- 组 43 关着利润门时看不出问题；**再开时炸的是没带本修复的旧二进制。**
