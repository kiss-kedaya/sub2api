## 修复

- 修前：充值页显示 `EPUSDT（USDT）`。修后：统一显示 `USDT`；后台标识、请求参数 `epusdt`、币种汇率均不变。
- 修前：使用记录页有 1680px 宽度上限，长名称被挤压。修后：取消上限，仅在共用 UsageTable 覆盖 `max-content` 最小宽度。

## 风险与已知问题（重要）

- **首次 1% 灰度时旧机候选 `NRestarts=7`，立即回退到 336。** 核对 journal 确认 7 次退出都发生在**接流前的启动阶段**（UTC 14:28:58–14:29:35）。
- 旧机临时端口范围 1024-65535，保留列表漏掉了在用端口，已补 6074/6076/8096/8098。
- **原始启动失败正文未留存**：标准日志桥会把 `Failed to...` 开头且无其他关键字的日志判为 INFO，被 error 级过滤。**端口抢占是已确认的配置风险，但不能说它是那 7 次退出的唯一直接证据。** 日志级别识别问题留作后续修复。
- 部分真实记录在 1440 宽下仍需表格内滚动，不能夸大为任意列数都不需滚动。
