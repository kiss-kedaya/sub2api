## 修复

- 修前：发布的二进制里版本号是构建常量，运行时显示的版本与实际发布 tag 不一致，排查线上问题时容易对错版本。
- 修后：构建时通过 ldflags 注入 `main.Version` / `main.Commit` / `main.Date`，二进制 `-version` 输出与发布 tag 一致。
- 自此每次发版同步 VERSION 文件，`/health`、后台版本展示和发布页三者对得上。
