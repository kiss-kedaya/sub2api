# 0.1.352 发布与部署记录

管理员账号页支持批量测模型：多选账号后可开弹窗，按单个或多个模型并发探测。DeepSeek 上游 `ref_code=400001` 且文案「已达到使用限制或余额不足」时，账号立刻 `status=error`、`schedulable=false`，并换号，不再被池模式早退吞掉。普通参数 400 不停号。无数据库迁移，无价格、倍率、支付或分组变更。`DATABASE_MIGRATION_MODE=validate`，预扣保持关闭。

351 留着当备份，进程不关。

## 发布

- 功能提交 `a7bf3c5dc`、`ac81d0c37`，版本提交 `575e5e640`。二进制 SHA256：`a4043a7d985a418e2cbdc53b7a6d37d86561bbe1764cce1acde59e22df83d21e`。
- 前端入口：`/assets/index-xst0NDWy.js`、`/assets/AccountsView-D4LwHa59.js`、`/assets/index-DR6UDIe9.css`。

## 滚动上线

- 候选：352 / 内网 8214 / pprof 6108。稳定回退：351 / 8212。350 / 8210 仍作 1% 阶段 backup。349 / 8140 进程仍在，已不在 nginx。
- 1% 约 3 分钟，352 有真实 200（新机 57 / 旧机 60），无平台 panic，NRestarts=0。
- 两台切到 352 全量。351 作为 nginx backup。新机静态入口切到 `index-xst0NDWy.js`，351 入口备份在 `/opt/sub2api/ui/index.html.351`。
- 无连接的 345 / 347 / 348 已停。349 / 350 / 351 进程仍在，不关。

| 节点 | 业务流量比例 | 352 PID | 保留的 351 PID | 启动时 NRestarts |
| --- | --- | --- | --- | --- |
| ns575199 | 40% | 2929205 | 2764983 | 0 |
| ns572368 | 60% | 3065502 | 3053215 | 0 |

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-352-20260924/rollout352_guard.py rollback`。新机命令前加 `sudo -n`。只切回 351 流量和静态入口，不停应用、不删旧资源。
