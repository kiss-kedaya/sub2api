# 0.1.339 发布与部署记录

利润门切组后仍吃入口组阈值，DeepSeek 等未开门分组被 43 组否决。339 切组时重绑利润门，门为空则清掉入口残留。Grok 未映射的 `grok-4.7` 可落到同平台 Grok 号；Responses 上游强制 stream；入站 `/v1/responses` 不再预降级 Chat。Codex `local_shell_call` 回桥。

无数据库迁移，无价格、倍率、支付或分组开关变更。`DATABASE_MIGRATION_MODE=validate`，`GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`。旧机 `DATA_DIR=/opt/sub2api/data-316`。未开 `GROK_UPSTREAM_DUMP`。318-337 中除 336 backup 外已停，338 全程不关。

## 发布

- 标签 [v0.1.339](https://github.com/kiss-kedaya/sub2api/releases/tag/v0.1.339)，提交 `225362cf5b8b32ce92743f01267189740f32d138`。
- 主提交 Security `35687482429` 成功；CI `35687482460` 失败。标签 Security `35687485477`、Release `35687485491` 成功；标签 CI `35687485446` 失败。
- 失败测：`TestForwardGrokResponsesRetriesInvalidEncryptedContentOnce`（会话 ID 改走缓存头，body 不再带 `previous_response_id`）；`TestSmartRouteLazyCatalogPreservesSelectionOrder/absent_models_are_skipped`（未映射 GPT 按官方逻辑打同平台组）。未作为回退条件。
- GitHub Linux amd64 归档 SHA256：`70752399ebab08f27e013372293429086721f1476880acc67a380c0d14514047`。
- 两机生产二进制 SHA256：`b93629a9b5eb090fa019f738dbe9fd321fd6581b25636bf8525f5dfee23c9040`。
- 前端无改动，仍为 338 入口 `/assets/index-DlKpmTe8.js`、`/assets/index-BgSWdc1U.css`。

## 滚动上线

- 候选：339 / 内网 8120 / pprof 6082。稳定回退：338 / 8100。336 留在 8096 作二级 backup，不接主流量。
- 流量比例保持旧机 40%、新机 60%。1% 灰度：338 权重 3960/5940，339 权重 40/60。
- 旧机先替换昨天热修 339（SHA `e6003e88...`，compact hotfix）为正式包后再接流。新机 UTC 04:44 已换正式包。
- 1% 灰度约 7 分钟。切全量前旧入口 339 业务成功 53 次，新入口 51 次；无 `openai.responses_panic_recovered`，无新增平台 panic。
- 旧机 UTC 05:04:37、新机 05:04:35 切至 339 全量。338 作为 nginx backup，进程不关。

| 节点 | 业务流量比例 | 339 PID | 保留的 338 PID | 336 PID | 启动时 NRestarts |
| --- | --- | --- | --- | --- | --- |
| ns575199 | 40% | 1532627 | 584902 | 3184254 | 0 / 0 |
| ns572368 | 60% | 2766254 | 2677876 | 2578466 | 0 / 0 |

## 验收

- 两台 origin 和公网 `https://kedaya.ai` `/readyz` 为 ready，首页 200。
- 守护 `watching / percent=100 / armed=true`。纯上游 502/503/429 不回退。
- 338 守卫已因配置外更改为 `disarmed`。339 守卫每 10 秒检查，失败只切回 338/8100，不停进程。

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-339-20260922/rollout339_guard.py rollback`，只切回 338 流量，不停应用、不删旧资源。
