# 0.1.351 发布与部署记录

工单回复从 Ctrl+Enter 改为 Enter 发送，Shift+Enter 换行，输入法选字不误发。列表只留全部 / 未关闭 / 已关闭，去掉接单、分配、优先级和手动状态。无数据库迁移，无价格、倍率、支付或分组变更。`DATABASE_MIGRATION_MODE=validate`，预扣保持关闭。DeepSeek 400001 停号换号未进本包。

350 留着当备份，进程不关。

## 发布

- 功能提交 `fa255e904`，版本提交 `3bf4ea467`。二进制 SHA256：`b639f84ef6c4b37a345327d88195dc1276ce061c8f6ed196e08c194268d493e4`。
- 前端入口：`/assets/index-buE-iNc7.js`、`/assets/TicketsView-BOoG0S55.js`、`/assets/index-DR6UDIe9.css`。

## 滚动上线

- 候选：351 / 内网 8212 / pprof 6106。稳定回退：350 / 8210。349 / 8140 进程仍在，已不在 nginx。
- 1% 约 5 分钟，351 有真实 200，无平台 panic，NRestarts=0。
- 两台切到 351 全量。350 作为 nginx backup。新机静态入口切到 `index-buE-iNc7.js`，350 入口备份在 `/opt/sub2api/ui/index.html.350`。

| 节点 | 业务流量比例 | 351 PID | 保留的 350 PID | 启动时 NRestarts |
| --- | --- | --- | --- | --- |
| ns575199 | 40% | 2764983 | 2618012 | 0 |
| ns572368 | 60% | 3053215 | 3042292 | 0 |

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-351-20260924/rollout351_guard.py rollback`。新机命令前加 `sudo -n`。只切回 350 流量和静态入口，不停应用、不删旧资源。
