# 0.1.343 发布与部署记录

OpenAI 平台新增账号类型 Cloudflare。后台填 Account ID 和 API Token，不用手填 Base URL。模型同步走 Cloudflare 的模型搜索。对话测试、OpenAI 兼容和 Anthropic 原生都转成 Chat Completions，按上游返回的 token 走原来的扣费。`count_tokens` 在本地估算，WebSocket 和原生 Responses 不会把 Cloudflare Token 发到 `api.openai.com`。无数据库迁移，无价格、倍率、支付或分组开关变更。`DATABASE_MIGRATION_MODE=validate`，`GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`，预扣保持关闭。旧机 `DATA_DIR=/opt/sub2api/data-316`。342/341/340 进程全程不关。

`typesafe/jev` 不是对话模型，这次上线不会让它出字。测 Cloudflare 要用 Workers AI 的对话模型，例如 `@cf/meta/llama-3.1-8b-instruct`。

## 发布

- 标签 [v0.1.343](https://github.com/kiss-kedaya/sub2api/releases/tag/v0.1.343)，提交 `884f65e4f`。
- 两机生产二进制 SHA256：`cef33141a0f92db30cbb9048c4f0df16826c5a8bb6ae0de639af6110aa20d1c8`。
- 前端入口：`/assets/index-BsacjGPp.js`、`/assets/index-ihyzV4JN.css`。新机静态入口 SHA256：`e70213140ed66082de3e53f21a0e5f6492ade1bfee7383b79b441ff9b766253e`。

## 滚动上线

- 候选：343 / 内网 8128 / pprof 6090。稳定回退：342 / 8126。341 留在 8124 作再上一层，不接主流量。
- 流量比例保持旧机 40%、新机 60%。1% 灰度：342 权重 3960/5940，343 权重 40/60，backup 8124。
- 切 1% 前先停 342 守卫。
- 1% 约 6 分钟。切全量前旧入口 343 业务成功 122 次，新入口 67 次；无平台 panic，NRestarts=0。
- 旧机 UTC 13:32:52、新机 13:32:53 切至 343 全量。342 作为 nginx backup，进程不关。341 进程不关。

| 节点 | 业务流量比例 | 343 PID | 保留的 342 PID | 341 PID | 启动时 NRestarts |
| --- | --- | --- | --- | --- | --- |
| ns575199 | 40% | 3690735 | 420572 | 1868831 | 0 / 0 / 0 |
| ns572368 | 60% | 2837062 | 2810106 | 2801237 | 0 / 0 / 0 |

## 验收

- 两台 origin `/readyz` 为 ready。两边公网 Host 探活 ready。新机静态入口和旧机嵌入入口都是 `index-BsacjGPp.js`。
- 守护 `watching / percent=100 / armed=true`。纯上游 502/503/429 不回退。
- 342 守卫已停。343 守卫每 10 秒检查，失败只切回 342/8126，不停进程。
- 后台刷新后，OpenAI 账号创建里能看到 Cloudflare。填 Account ID 和 API Token 才能保存。

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-343-20260922/rollout343_guard.py rollback`，只切回 342 流量和静态入口，不停应用、不删旧资源。
