# 0.1.344 发布与部署记录

OpenAI 平台的 Cloudflare 账号，点「同步上游支持的模型」时会带上 Account ID。不再把 Base URL 填成 `https://api.openai.com`。模型搜索最多翻 20 页。无数据库迁移，无价格、倍率、支付或分组开关变更。`DATABASE_MIGRATION_MODE=validate`，`GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`，预扣保持关闭。旧机 `DATA_DIR=/opt/sub2api/data-316`。343/342/341 进程全程不关。

`typesafe/jev` 不是对话模型。测 Cloudflare 要用 Workers AI 的对话模型，例如 `@cf/meta/llama-3.2-3b-instruct`。

## 发布

- 标签 [v0.1.344](https://github.com/kiss-kedaya/sub2api/releases/tag/v0.1.344)，提交 `58e91fc50`。
- 两机生产二进制 SHA256：`772329bddc884f1c0945267c2d5bf5393694b777221b5e5f0ff549e1a473f7e4`。
- 前端入口：`/assets/index-CvqdRKgG.js`、`/assets/index-ihyzV4JN.css`。新机静态入口 SHA256：`212b70563545f737fb353bce7ccdb4825f7ef4edf67020a105de45e2a1cb0d19`。

## 滚动上线

- 候选：344 / 内网 8130 / pprof 6092。稳定回退：343 / 8128。342 留在 8126 作再上一层，不接主流量。
- 流量比例保持旧机 40%、新机 60%。1% 灰度：343 权重 3960/5940，344 权重 40/60，backup 8126。
- 切 1% 前已停 343 守卫。344 守卫继续每 10 秒检查。
- 1% 约 11 分钟。切全量前旧机 344 业务成功 226 次，新机 85 次；无平台 panic，NRestarts=0。
- 新机先换成 `index-CvqdRKgG.js`，并写下 `ui-active.json`。旧机 UTC 14:45:38、新机 14:45:36 切至 344 全量。343 作为 nginx backup，进程不关。342/341 进程不关。

| 节点 | 业务流量比例 | 344 PID | 保留的 343 PID | 342 PID | 启动时 NRestarts |
| --- | --- | --- | --- | --- | --- |
| ns575199 | 40% | 122584 | 3690735 | 420572 | 0 / 0 / 0 |
| ns572368 | 60% | 2843041 | 2837062 | 2810106 | 0 / 0 / 0 |

## 验收

- 两台 origin `/readyz` 为 ready。两边公网 Host 探活 ready。新机静态入口和旧机嵌入入口都是 `index-CvqdRKgG.js`。
- 守护 `watching / percent=100 / armed=true`。纯上游 502/503/429 不回退。
- 343 守卫保持停止。344 守卫每 10 秒检查，失败只切回 343/8128 和旧静态入口，不停进程。
- 后台刷新后，OpenAI 选 Cloudflare，填 Account ID 和 API Token，点「同步上游支持的模型」应列出 `@cf/...`，不再报 account id 长度错误。

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-344-20260922/rollout344_guard.py rollback`。新机命令前加 `sudo -n`。只切回 343 流量和旧 `index.html`，不停应用、不删旧资源。
