# 0.1.325 Grok 自定义工具增量输出

日期：2026-09-16。正式仓库 F:\GO\sub2-official-work。

## 证据与边界

- 323 上线后用户再次报告 Grok 首字接近总耗时。约 30 分钟样本中，升级前后均有此现象：29145 升级前 72/2333、后 20/1103；29155 升级前 26/3046、后 13/1613。判定为 first_token_ms >= duration_ms - 50，不等于每条都由同一缺陷造成。
- 普通 Chat 和 Responses 公网实测均正常分段：Chat 首正文 23.388 秒、总 27.404 秒、371 正文片段；Responses 23.483/27.027 秒、332 片段。不能把少数样本解释成全站流式失效。
- 自定义工具真实请求 97444ebe-1157-9bc1-b7c8-d53a0b7e3c2a：推理和正文正常流式，5717 字符 custom 参数在 75.581 秒仅一个 delta 后紧跟 done，总 76.878 秒。代码确认旧 Restorer 吞掉所有 function_call_arguments.delta，直到 done 才还原 custom 输入。
- function 对照请求产生 message 后达到输出上限，未产生工具调用；不能用它们证明上游参数是否分片。确定性服务测试使用分片参数且扣住终态，确认转换层缺陷与修复效果。
- semantic TTFT 还会受到 output_item.added 到达时间影响。不得把此次修复描述为消除了所有首尾相等样本，或通过修改计时口径掩盖上游等待。

## 修复与审查

- 首个根 input 字符串按新到字节增量处理，解码委托 encoding/json。跨片转义、UTF-8、Unicode 代理对不拆坏，不把 JSON 包装发送给客户端。
- 根键不是 input、非规范结构或过长前缀保持完整参数兜底，不误取嵌套 input 或字符串中的同名文本。
- 每个下游事件连续编号、保留 item/call ID；参数 done 仅补尾，不重复内容。省略工具 done 的成功响应快照也补齐生命周期。
- 完成后的参数快照必须完全一致，变更会明确返回协议错误，不能静默下发矛盾参数。失败/取消终态不伪造成功工具结果。
- 计费、缓存、重试预算、数据库和服务器容量配置不变。tool_search 没有自由文本 delta 契约，本轮不强行转换。
- 首版 324 已取消构建，没有发布包、没有部署。324 的完整前缀重扫和根键匹配风险在上线前被发现，325 改成正常路径 O(N) 扫描；不移动旧标签。

## 本地验证

- EverySplit 覆盖每个分割点、转义键、控制字符、孤立及成对 surrogate、嵌套字段。另测逐字节 UTF-8、非法转义、终值变化、重复 done、缺 done 和成功终态快照。
- 同一机器 64KiB/16 字节分片微基准：未上线 324 约 167.5ms、595.9MB 分配；线性实现约 1.16ms、2.45MB。单次微基准不是线上吞吐收益保证。
- 三包 unit 完整测试曾通过 service 210.865 秒、handler 138.992 秒、apicompat 0.781 秒；最后收尾边界变更继续由定向 race 和最终 CI 验证。
- 两名独立代理完成只读审查与独立服务回归。确定性服务测试通过完整 forwardGrokResponses 路径，终态释放前收到参数分片，最终参数无重复、序号连续、usage/cache 保留。

## 部署

- 候选 325 最终使用 8093/pprof6071，323 8091 在线热备，物理旧40%/新60%保持。8092启动冲突的原因见下文。
- 复用323已校正的守护规则：上游业务5xx不回退，平台健康失败回退；保留应用与长连接。
- 最终CI、发布与双机灰度已完成，证据见下文。两机均保持自动健康回退和323热备。

## 发布前追加验证

- Release 35060529110 成功，源码 2ea925e9be611a1862fd89228fcdb63734cf2b14。归档 SHA256 abfcdeed6ce39ea04f9f2becdc8a42fa01ea36bb3bbafa43e5e47633d70d3512；两机下载并校验的二进制 SHA256 均为 e6b90750ada6cf36ad870dcfa758b63fe292a8e2940fa900329e18f1988b794e。
- 初次 main/tag CI 35060470390/35060529113 仅 HTTP bridge 的 ResponseDone 测试失败：成功快照补齐 output_item.done 后应为5帧，旧断言仍为4帧。87e0fb8e2 只修改测试，校验完整类型顺序、连续序号、参数及工具完成状态；定向 race 通过。除自动 VERSION 同步外，运行时代码与325发布包一致，不移动标签或替换发布包。
- 本机完整 unit 首次因 PATH 缺 sh 导致 repository 的 pg_dump mock 失败；补充已有 Git/bin 到当前进程 PATH 后 repository 全包通过，不修改业务实现迁就环境。最终完整 CI 以35062420915为准。
- apply_patch 工具样本成功触发目标分支，测试只接收工具调用，未执行模型生成的补丁。323请求91050c1a-c03a-9011-997f-c2314c2562c2：1366字符 custom 参数只有1个delta，7.266秒到达、7.267秒completed，序号及内容一致。
- 直连上游29145的 function 对照也只有1个参数delta：1397字符于4.771秒到达、4.786秒completed。它说明该上游确实存在整包晚发，而非全部由网关造成。325只消除网关可控的额外等待，不能凭跨请求耗时差宣称消除上游延迟。

## 候选启动与灰度

- CI35062420915包含unit、integration、lint、frontend、shell全成功；安全35062421035成功。本机补齐已有sh路径后完整unit也通过。
- 新机最初候选启动正常。旧机启动失败不是画布警告导致：`ss -tanp sport = :8092`确认323进程779457的Redis出站连接占了10.254.0.2:8092，而监听预检只看LISTEN漏掉了ESTAB。没有中断该连接。
- 两机候选改用已核验空闲的8093；仅在原ip_local_reserved_ports基础上追加6071、8093，防止临时出站端口抢占。旧机原值5678,7100-7103,30021-30025保留；新机原值为空。持久化文件/etc/sysctl.d/99-sub2api-rollout-ports.conf，未调整容量、端口范围或数据库配置。
- 初始安装备份：新机/root/sub2api-install325-20260916T062108Z，旧机/root/sub2api-install325-20260916T062119Z。端口修改备份：新机/root/sub2api-325-port-fix-20260916T062817Z，旧机/root/sub2api-325-port-fix-20260916T062810Z。旧机临时info日志诊断结束后已恢复原环境。
- 当前候选PID新2095321、旧907973；原323 PID2086159/779457和322 PID2056026/261660均不变。只停止过尚未接流的325候选，原实例未重启。
- 更新8093后两机各24项守护测试全通过。守护SHA256 d4625939da837b50fcd92f702140fd3abb59880fd7b3e7e68c03026ac483784f。北京时间14:30:47/55分别开始1%，只交接323守护timer，不停323应用。
- 候选真实Responses文本请求10482515-3cf4-9bd2-9dd3-96278eeac82c：首正文58.157秒、总76.023秒、378片、31个100ms窗口，序号缺失/非法为0，缓存2048与usage账单一致。Chat请求95f1fb50-4fd7-43d9-9c4c-5c94233e49e3：54.804/70.961秒、349片、缓存128一致。正文首字与后台semantic TTFT不是同一指标，不能混用。
- GPT候选两机真实Responses首正文2.497/1.762秒、总10.937/9.848秒，缓存6784/4224与账单一致，无缺失序号。
- 候选custom工具两机均完成，内容1366字符无重复/冲突、序号正常；实测上游仍只发一片，所以本次真实工具样本不能证明多片情况下的提前量。多片保障由扣住终态的服务回归验证。

## 用户澄清后的双Responses审查

- 用户明确/chat非流强制流式符合预期，本轮保持。关注点为/v1/responses入站到同名上游接口的流式请求，不能只凭后台stream标记验收。
- 15分钟最多6000条只读样本：29145双Responses流式2421条中104条first_token_ms>=duration_ms-50，29155为1443中9条。异常样本多数短输出与长上下文，尚不能仅凭usage确定每条的具体事件类型。
- 两条上游直连同样只有一个function参数delta；额外短工具对照明确发送stream=true、返回Content-Type=text/event-stream，但参数4.454秒才到、4.456秒completed。SSE与工具参数晚发可同时存在，耗时接近不等于请求变成非流。
- 新增完整svc.Forward回归，含/不含custom工具两种输入：实际出站/v1/responses、stream为JSON true；终态扣住时正文已经flush；completed后不等待EOF，缓存96保留。该测试与custom增量服务测试race通过（5.466秒）。未改计时口径，未改/chat行为。
- 记录一个后续诊断缺口：启动日志`Failed to start server`可能在error级配置下被标准日志文本推断当info过滤；不要把最后一条画布警告当成根因。未在此修复中扩大改动范围。
- 独立只读复核未发现新增上线阻断。额外风险仅为读取额度响应头后同步UpdateExtra可能推迟首片读取；当前没有慢存储证据，不作为本轮已确认根因，不盲改异步持久化。

## 全量后的公网验证

- 两台入口于北京时间14:43:27/34切100%；323在8091保持热备，不停止存量长连接。物理旧40%/新60%未改。守护保持platform_health_only。
- 公网Grok Responses请求743c5b29-9472-97ff-99e9-8b82552206fb：HTTP200/SSE/completed，首正文31.119秒、总35.071秒，344片，缺失/非法序号0。semantic首字账单2.496秒对应较早的推理输出，不能拿正文时间替换后台指标。下游缓存2176与账单一致。
- 公网custom请求a7a794e0-31d4-97ab-a04b-25b5b41cc3f2：1366字符参数于7.276秒到达，input.done于7.843秒，completed于8.414秒；实测不再等工具done才下发，序号完整递增、delta/done/item/terminal内容一致。上游仍只给一片，不宣称该样本为多片流式。缓存512与账单一致。
- 公网GPT Responses请求9534fd76-268b-4f03-a718-a2f801a6ba60：HTTP200/completed，首正文3.280秒、总9.863秒、281片，序号正常，缓存4224与账单一致。
- 北京时间14:54:24/29通过最终验收，两机phase=verified、percent=100、armed=true，已全量观察约11分钟，成功守护检查60/56次；公网版本0.1.325、readyz/health正常，nginx配置校验通过。325/323均无重启、panic或OOM。部署没有关停323存量连接。

## 内存热点与未消除风险

- 接流后RSS显著增长，因此延长观察并抓取两次heap和一次goroutine快照，未调高现有GOMEMLIMIT=16GiB、MemoryMax=20G或守护RSS17GiB阈值。
- 14:47普通heap样本：新机inuse约3.79GiB、旧机约3071MiB；14:51带gc=1样本：新机6.75GiB、旧机4.97GiB。主要分配栈readRequestBodyChunks占约69%/71%，不是custom增量解码。单次GC快照不是泄漏证明，也不能据此宣称已排除泄漏。
- 新机14:53协程28245，其中约5974个HTTP handler，约5104个Client.Do/Transport.RoundTrip等待；4821落在通用doOpenAIUpstreamWithHeaderCancel，forwardGrokResponses仅174。内存压力主要与大量长请求携带请求体等待上游相关，需要后续按上游/请求生命周期做容量审查，不以本次Grok工具修复名义随意缩超时或改并发。
- 14:54验收新机RSS约15.4GiB、旧机13.6GiB，主机可用约20.9/19.8GiB。新机RSS开始在15GiB附近波动，仍是需持续关注的风险；没有声称内存泄漏彻底修复。守护继续自动检查平台健康、候选进程与内存余量。
- 原始性能快照存于本地.artifacts/heap325-new.pb.gz、heap325-old.pb.gz、heap325-new-gc.pb.gz、heap325-old-gc.pb.gz、goroutine325-new.pb.gz，仅用于内部分析，不随公开release上传。
- 14:59双机守护仍verified/100%/armed；RSS约15.3/15.4GiB，未触阈值。两机经公网readyz正常。本地Windows出口访问返回Cloudflare地域403，响应明确为不开放中国大陆访问；这是边缘访问策略，未修改，也未误归为325平台故障或触发回退。
