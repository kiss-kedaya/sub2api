# 322探针误回退排查与恢复

## 确认事实

- 北京时间2026-09-16 09:55复查：两入口已从322回退321。322与321进程PID、NRestarts均未变化。此前04:33汇报全量属当时状态，不能代替数小时后的持续实况。
- 新机05:25:27回退：公网/readyz的一次curl五秒超时（exit28）。相同两分钟窗口新机入口1478条健康请求全200、最大16ms；旧机入口1501条全200、最大17ms。无法证明超时在CDN、链路还是客户端哪个节点发生，但没有源站平台故障证据。
- 旧机09:25:42回退：09:25:29有一条/health返回499，请求耗时0、upstream_status为“-”；这是客户端在收到健康响应前取消。该两分钟窗口其余1478条探针均200，最大203ms。守护主动检查当轮通过，随后错误地把被动日志中的499当平台失败。
- 两机当前内网322 /readyz均ready且版本为0.1.322；公网入口版本为0.1.321，证实是流量回退，不是发布二进制失败。

## 最小修复

- 被动健康日志保留499计数，但499不触发平台故障回退；其他非200规则保留。
- 公网探针仅exit28超时允许同轮复测一次，复测前重新确认本机入口及两台候选/readyz。候选或本机入口失败仍立即回退，公网再次失败仍回退。HTTP5xx、非JSON或错误状态不走超时重测。
- 原上游业务502/503只记录，仍不作为版本回退依据。未改服务器容量、数据库、业务代码或应用二进制。
- 将已使用脚本归档到deploy/rollout，24项守护测试与5项恢复事务测试通过，新增499与单次公网超时用例修前失败、修后通过。独立代理复核恢复脚本后，补齐固定配置SHA、保留新回退原因、校验/加载/安装同份字节三项保护。
- 恢复脚本持原守护锁，校验回退状态、配置归属、旧PID、两版健康与内存余量，先备份脚本/状态/PID/配置再安装。重新1%灰度，保留旧连接与321原进程；phase备份目录使用唯一时间戳，不覆盖旧回退证据。

## 恢复结果

- 北京时间10:09:54新机、10:10:02旧机恢复1%灰度，321继续99%，所有应用原PID不变。新守护SHA256：09e5789844707766ce9f193436b5eb19492ad920fd0b4e008bdc9158fc7c8f1e。
- 恢复备份分别为原守护目录下resume-backup-20260916T020954Z与resume-backup-20260916T021000Z，包含旧脚本、状态、PID、最近检查和切流前配置；原回退证据未删除。
- 两机Chat/Messages/Responses共6条真实input_too_small均400/400且正确分类。Grok候选HTTP200，首正文23.422秒、总27.077秒，358片段、缓存2048。
- 稳定高缓gpt-5.5候选和旧321对照均出现125秒测试截止前无法完整结束；321样本未收到正文，322部分样本收到正文后截止。该线路延迟问题仍在，不能说所有GPT请求已无误，也不能因旧新版共有的上游等待再次盲回退。
- 待补充全量与持续观察结果。原321/322历史派生统计校正收据不得删除或重复执行。
