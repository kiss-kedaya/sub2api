# Kiro 分组 404 排查与修复

日期：2026-09-16。正式仓库：F:\GO\sub2-official-work。

## 结论与证据

- Kiro 是 group_id=10 的 Anthropic 分组名，关联账号都是 apikey。没有证据支持新增 Kiro 平台或修改账号类型。此前类型判断不成立。
- 19:22 只读样本中，账号 28267 五分钟有 2379 次 404，原始响应是标准 nginx HTML 404 页面。请求路径为 `/v1/messages`，数据库保留了对应上游 404；不能为了迎合描述而修改错误来源。
- 同账号直连 Messages（带或不带 beta 参数）及 Chat 都返回 Cloudflare 520；其他账号相同 Messages 请求正常返回 200/SSE。因此既存在故障线路，也存在网关没有绕过该线路的容错缺陷。
- 网关共享分类器把 HTML 路由 404 当成客户端错误，正常转发和协议转换路径直接返回给用户，没有让 handler 选下一账号。
- 暂停故障线路后，暴露第二类本地 404：`claude-opus-4.5`、`claude-opus-4.6` 原先仅故障账号配置，健康账号缺少别名。保留错误来源区分，两类问题不能混成一种。

## 线上止血

- 19:26:30 通过现有管理 API 暂停账号 28267 新调度，保留账号和正在执行的请求。原值备份 `/root/sub2api-kiro404-20260916T112630Z/schedulable-before.json`，可经同一管理接口恢复。
- 19:33:44 给账号 29168 补两个已实测的映射：`claude-opus-4.5` 到 `claude-opus-4-5-20251101`；`claude-opus-4.6` 到 `claude-opus-4-6`。原映射备份 `/root/sub2api-kiro-aliases-20260916T113344Z/mapping-before.json`。
- 修改前分别直连上述两个目标模型，均为 200/SSE/message_stop。修改后逐字段核对 credentials，除新增两个映射外完全一致。未改余额、倍率、密钥、数据库结构。
- 公网原模型请求 `3890fced-5329-4b1d-aaf6-422d1e078936`：200，首正文 3.251 秒、正常 message_stop。
- 公网 `claude-opus-4.5` Chat 请求 `c82761d4-20d4-43d0-aa1e-cc1d11b8ae4d`：200/SSE，4 个事件，正常 DONE。
- 公网 `claude-opus-4.6` Messages 请求 `991288f6-3944-4763-a63e-7d01c19a54e7`：200，首正文 1.783 秒、总 1.976 秒，正常 message_stop。
- 后续三分钟样本有 677 条成功 usage，新增 404 为 0；仍有其他账号的容量 429 和少量上游 502，不宣称整个分组所有错误归零。

## 代码修复边界

- 仅识别 HTTP 404 且完整 HTML 文档开头、标准 404 title/h1、nginx 页脚特征。不依赖可缺失的响应头，不把任意 JSON、正文中的 HTML 或资源不存在错误当线路故障。
- 确认路由故障后交给现有 handler 换账号，首个失败响应不提交给下游。
- Anthropic 普通与透传的内部重试遇到该响应立即退出，避免自定义错误码配置导致同线路最多重复五次。
- OpenAI 透传复用同一窄判定；统一 failover 构造器禁止被池模式再次打开同账号重试。
- 保留显式临时不可调度规则，但该路由响应不能被自定义404规则永久标记凭据错误。普通模型不存在/文件不存在/响应不存在仍走原逻辑。
- 不新增平台，不更换模型能力，不改缓存计费、流式输出或首字计时。

## 回归与部署状态

- 修复前分类测试稳定复现：预期线路故障，实际 client。
- 四条 Anthropic 转发路径、三种错误码策略回归：返回可换号错误，下游未提交，同账号调用一次，不永久停号。另覆盖 OpenAI 错误构造器及正常资源404负例。
- 完整服务209.039秒、handler139.508秒、apicompat测试通过。独立复核找出自定义错误码测试必须模拟JSON数字float64，已修正并加分支前置断言，最终定向race4.787秒通过。
- 326复用325平台健康回退策略，候选端口8095/pprof6073，3238091保持原实例。两机24项守护模拟通过；未扩大内存阈值、超时或并发配置。
- 两机候选端口检查涵盖所有TCP状态；只在原保留端口列表追加6073/8095。备份分别为旧机 `/root/sub2api-326-ports-20260916T114043Z`、新机 `/root/sub2api-326-ports-20260916T114048Z`。

## 326 发布与灰度

- 源码/tag `57e178e7a152671dfd8af05f99e905cbc36feb04`；Release35091898651、main CI35091893220、tag CI35091898802全部成功，安全检查成功。完整Linux检查包含集成测试。
- 发布归档SHA256 `39f69117ec8d5182014b3a788bf16a59af890599c9cada4ff31c0de6b5c7aa97`；两机二进制SHA256均为 `2cefe0a588fdee7a9c8c0461e924f2b38078940ecfbb55d01a73d976eadf3541`，版本与源码提交一致。
- 326 PID旧1257623、新2108208；323原PID旧779457、新2086159保持。安装备份：旧 `/root/sub2api-install326-20260916T115857Z`、新 `/root/sub2api-install326-20260916T115905Z`。
- 19:59:49/55开始双机1%灰度，物理旧40/新60不变。守护脚本SHA256 `d680b803ad16efc795d0c0dfce4deb8e2926832a829ef076566ca64c22ded9cf`，平台健康失败回退323，上游业务错误仅观察。
- 候选Kiro Messages `608eed9d-c818-4e33-a806-02fb26f82ac9`：200，1.808秒正文、1.865秒完成；Responses `18d67a5c-1b28-4480-8ffb-813b2c1633a8`：200/completed；Chat `788a9322-db9e-4c24-952b-cdcce4000d1b`：200/DONE。
- 两机Grok custom请求均200/completed，序号缺失和冲突为0，delta/done/item/终态参数一致。旧机请求 `e285840d-465b-45d7-9044-050784e99149` 下游cache_read128；账单186096249对应 `client:6fcf5c10-6b83-42ad-93bf-9b96094439fc` 同为128。不要拿上游request_id直接查usage，优先用返回的client_request_id。
- 灰度日志的业务502包含账号29131已知422错误转换；已对照两条应用request_id与ops记录，属于上游HTTP错误，不据此回退整版。健康探针全200，无panic/OOM/重启。
- 新/旧入口20:04:44/50切100%，323继续在8091热备并保留存量连接。公网版本为0.1.326；公网Kiro Responses `84c7de24-3221-47bf-a567-faf395790512` 200/completed，总1.923秒。
- 旧325守护已确认rolled_back且未armed后停止timer，新326守护继续工作；没有停止任何应用。20:10:27/33完成全量超过五分钟验收，两机verified/100%/armed，原实例PID保持。后续600秒配置采用同一326二进制的另一组实例，见下文。
- 残留明确不支持的模型：sonnet4-8直连29168返回model_not_found、28663返回未配置官方价格。没有将它替换为其他模型，也不宣称该模型已可用。

## 325 回退复核

- 本轮确认生产接流为323，物理旧40%/新60%，325在8093保活。不得沿用14:59已全量的旧状态。
- 新机入口08:13:46 UTC与旧机入口08:13:55 UTC的就绪探针均由旧机325返回503，耗时约2秒；不是 nginx 自生503。
- readyz 的 PostgreSQL、Redis 检查共用2秒期限。同期PG有语句超时、旧机24核load约69.44；探针缺分项记录，不能确定哪一个依赖失败，也不能直接说325代码回归。
- 19:35 两机/入口/公网14项探针全200，两机323/325无重启。旧/新可用内存约28.9/26.2GiB。允许小流量验证，但保留平台健康异常立即回退，不扩大依赖超时阈值掩盖问题。

## 经验

- 分组名不是平台枚举。先查真实账号和失败请求，不凭名字补适配器。
- 暂停坏线路前后都要检查模型覆盖，避免别名只落在一个供应商上。
- 分类器的布尔字段不一定控制所有调用点；内部重试、池重试、永久处罚要分别核查。
- 版本上线结论有时效。每次接手重读 nginx 路由、守护状态和真实请求。
- 候选端口须查所有 TCP 状态，不能只看 LISTEN。本轮发现8094已被325的Redis出站连接占用，不中断该连接。

## 用户追加 Grok 响应头等待 600 秒

- 用户明确要求 `GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`。配置项已注册环境变量，校验范围0至1800秒；Grok转发使用HTTPUpstreamProfileGrok，transport读取此配置。原默认120秒。近期ops日志确认账号29131存在 `timeout awaiting response headers`。
- 两机 kedaya.ai 业务入口nginx的proxy_read_timeout均3600秒，本次不修改nginx超时、Go代码、流式idle或首字指标。600秒只控制上游响应头等待，不保证客户端/边缘代理也等600秒。
- 两机共享env及新实例专用env设为600，共享env逐行核对只新增该变量。启动新unit `sub2api-0.1.326-grok600-canary`，复用同一个已校验326二进制，HTTP8097/pprof6075。进程环境再次读取确认600。
- 旧/新新配置PID分别1276993/2110135，原326 PID1257623/2108208始终保活；323也未关闭。安装配置备份旧 `/root/sub2api-grok600-20260916T121617Z`、新 `/root/sub2api-grok600-20260916T121625Z`。
- 8097/6075只在原保留端口列表追加，原值均备份，未断旧出站连接。备份旧 `/root/sub2api-326-grok600-ports-20260916T121410Z`、新 `/root/sub2api-326-grok600-ports-20260916T121415Z`。
- 守护路径 `/opt/sub2api/rollout-326-grok600-20260916`，SHA256 `f271ba0439c201377528ba15afef1ba594e2d9da5fdcee5c1896313595d9f86b`。复用已验收守护并把回退目标设为原3268095；两机各24项模拟通过，交接时只停止原守护timer，不停应用。
- 20:18:42/49开始1%灰度；20:43复核旧462/新421成功业务、健康探针旧173/新202全200，无重启、panic或OOM。20:44:22/31切100%，物理旧40/新60不变。
- 新配置候选Grok两机均200/completed，缓存128、序号完整；全量公网请求796f284c-2dbc-4370-87f1-c705c430449a正常结束。额外非流式公网请求bb58839c-ebed-416b-983d-d7451132f926明确发送stream=false，返回200/application-json/completed，总2.152秒，缓存128。该短请求不能证明真实上游等待超过120秒的表现，只证明配置与正常非流式路径均正常。
- 20:43 Kiro最近五分钟952次成功、两类404均为0；20:47再次复查948次成功、两类404仍为0。
- 20:50:07/15完成600秒配置全量超过五分钟验收，两机verified/100%/armed，公网版本仍为0.1.326；守护成功29/33轮。新实例与原326/323均无重启，未停止或删除应用。
- 验收时新配置RSS旧4201036KiB、新7119948KiB，主机可用旧27693000KiB、新17132680KiB。健康回退继续启用，不因上游业务422/502/520回退。短观察不能证明长期内存风险消除。
