# 0.1.347 发布与部署记录

撤掉尚未作为全量上线的 `typesafe/jev` 路由、使用弹窗和 SKILL。Cloudflare 账号类型保留。无数据库迁移，无价格、倍率、支付或分组开关变更。前端入口仍是 `index-CvqdRKgG.js`。`DATABASE_MIGRATION_MODE=validate`，`GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`，预扣保持关闭。

336 到 344，以及已经没有连接的 346，两台都已停掉并禁用。345 留着当备份，进程不关。

## 发布

- 本地提交在版本文件改为 `0.1.347` 之后。二进制 SHA256：`0967422c62cc9ed3ebd4c41b3fddf2fd4db5a52033823d1ecd8a48c2d814d174`。
- 前端入口不变：`/assets/index-CvqdRKgG.js`、`/assets/index-ihyzV4JN.css`。

## 滚动上线

- 候选：347 / 内网 8136 / pprof 6098。稳定回退：345 / 8132。
- 先把 346 的 1% 流量切回 345。1% 灰度大约 9 分钟，347 有真实 200，无平台 panic，NRestarts=0。
- 两台切到 347 全量。345 作为 nginx backup。

| 节点 | 业务流量比例 | 347 PID | 保留的 345 PID | 启动时 NRestarts |
| --- | --- | --- | --- | --- |
| ns575199 | 40% | 998614 | 189801 | 0 |
| ns572368 | 60% | 2910606 | 2848829 | 0 |

## 验收

- 两台 347 `/readyz` 为 ready。旧机公网 Host 探活 ready。切全量后 8136 上有活动连接。
- 守护 `watching / percent=100 / armed=true`。纯上游 502/503/429 不回退。
- 后台 OpenAI 仍可选 Cloudflare。使用弹窗没有 `typesafe/jev` 页。

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-347-20260923/rollout347_guard.py rollback`。新机命令前加 `sudo -n`。只切回 345 流量，不停应用、不删旧资源。
