# 0.1.341 发布与部署记录

本地账号并发打满时直接把 `Concurrency limit exceeded` 甩给用户，401 黏连死号反复打坏 key，DeepSeek `/v1/embeddings` 被路由门 404。341 忙号排除后换号，全组忙才返回 `gateway_concurrency_limit`；401/凭证错误允许粘性会话让位；DeepSeek embeddings 与 Responses 兼容平台同一放行集合。Grok 视频 status/content 仍绑创建号，忙时等待不换号。

无数据库迁移，无价格、倍率、支付或分组开关变更。`DATABASE_MIGRATION_MODE=validate`，`GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`，预扣保持关闭。旧机 `DATA_DIR=/opt/sub2api/data-316`。340/339 进程全程不关。

## 发布

- 标签 [v0.1.341](https://github.com/kiss-kedaya/sub2api/releases/tag/v0.1.341)，提交 `4162a5eb8`。
- 主提交 Security 成功；CI 失败。标签 Security、Release 成功。
- 失败测与 339/340 相同：`TestForwardGrokResponsesRetriesInvalidEncryptedContentOnce`、`TestSmartRouteLazyCatalogPreservesSelectionOrder/absent_models_are_skipped`。未作为回退条件。
- 两机生产二进制 SHA256：`ccaf9421ddede08a5183d6a0f9bd21ad7eb0f20d1e04dde899df3c5b17a9e3ba`。
- 前端入口仍为 `/assets/index-D2jFBANy.js`。

## 滚动上线

- 候选：341 / 内网 8124 / pprof 6086。稳定回退：340 / 8122。339 留在 8120 作二级 backup，不接主流量。
- 流量比例保持旧机 40%、新机 60%。1% 灰度：340 权重 3960/5940，341 权重 40/60，backup 8120。
- 切 1% 前先 disarm 340 守卫。
- 1% 灰度约 7 分钟。切全量前旧入口 341 业务成功 150 次，新入口 51 次；无 `openai.responses_panic_recovered`，无新增平台 panic，NRestarts=0。
- 两机 UTC 10:04:15 切至 341 全量。340 作为 nginx backup，进程不关。339 进程不关。

| 节点 | 业务流量比例 | 341 PID | 保留的 340 PID | 339 状态 | 启动时 NRestarts |
| --- | --- | --- | --- | --- | --- |
| ns575199 | 40% | 1868831 | 1570899 | active | 0 / 0 |
| ns572368 | 60% | 2801237 | 2771764 | active | 0 / 0 |

## 验收

- 两台 origin `/readyz` 为 ready。旧机公网 Host 探活 ready。
- 守护 `watching / percent=100 / armed=true`。纯上游 502/503/429 不回退。
- 340 守卫已 `disarmed`。341 守卫每 10 秒检查，失败只切回 340/8122，不停进程。
- 全量后可见账号 `29168` 上游 503 `service is busy`，属上游忙，不作为平台回退条件。

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-341-20260922/rollout341_guard.py rollback`，只切回 340 流量，不停应用、不删旧资源。
