# 充值入口开关与注册链接（2026-09-20）

- 加入 `payment_recharge_center_enabled` 设置，默认关闭。后台可开关充值页的外部支付宝 / 微信入口，入口名称不再含“充值中心”。省略字段不覆盖旧值。
- checkout-info 返回独立显示开关；关闭时不渲染或加载外部 iframe，充值停用且外部入口关闭时显示不可用。原有支付恢复、历史订单、订阅回调保留。
- USDT 支付方式区域加入用户指定的 OKX / Binance 注册链接，独立窗口打开，使用 noopener noreferrer。
- 已只读核对生产：原生支付宝、微信方式均关闭；只有 EPUSDT 实例启用。因此不改支付方式配置、provider、回调、订单或余额。
- EPUSDT 当前配置是 CNY 计价、USDT 收款。USDT 是支付媒介，不能把它误当成当前订单的计价币种。预览最初写入 USDT currency 与后端三字母法币契约不符，已修正预览为真实的 CNY 契约；未修改汇率与币种转换代码。
- Go 服务层、后台 GET/PUT 与省略字段保留测试通过。PaymentView 26 项、预览与隔离 29 项测试通过，locale 完整性、类型检查、相关 ESLint 和生产构建通过。
- 浏览器实际操作系统设置的开关和保存按钮，保存返回 200；重新进入充值页验证开 / 关。1440、375 宽的深浅主题无横向溢出，注册链接存在，关闭时没有 iframe。全部测试只使用本地模拟支付。
- 原本地预览地址：`http://127.0.0.1:4317/purchase`。这些改动现已随 0.1.336 上线，生产地址为 `https://kedaya.ai/purchase`。

## 生产验收（2026-09-20）

- 双机全量运行 336，公网版本和前端资源内容哈希匹配发布产物；观察约 51 分钟后确认完成。
- 真实 Chrome 检查 1440 / 375 宽、深浅主题；充值页只有 EPUSDT（USDT）支付入口，OKX / Binance 链接存在，支付宝 / 微信 tab 和 iframe 未加载。后台支付设置独立开关为 false。
- 深浅模式背景图片已内嵌并成功解码，线条显示正常；人工复核桌面充值页和管理员支付设置截图。
- 生产浏览器验收只读，没有切换开关、保存配置、创建订单或修改余额。此前开关保存测试为本地模拟，不冒充生产支付到账验证。
- 保留现有 CNY 计价、USDT 收款及汇率；334 实例和自动回退守护保留。部署细节见 [336 发布记录](release-336-20260920.md)。
