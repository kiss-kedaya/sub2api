# 0.1.323 流式超时与 Responses 必需字段修复

日期：2026-09-16。基于双机全量的 322。

## 已确认问题

- 真实上游账号 28659 返回顶层 `code=request_timeout` 的裸 `error`，内容为 `stream closed before response.completed`。此前未进入无输出故障转移，且裸错误没有登记最后 attempt，导致部分错误误归平台或沿用旧过载详情。原始证据见 `diagnosis-responses-stream-timeout-20260916.md`。
- 用户追加报告 `missing field sequence_number`。本地回归复现：协议转换首帧序号为 0 时被 `omitempty` 删除；HTTP/WS 合成失败和 compact 合成流也缺字段。旧 handler 注释及测试明确要求省略该字段，这与实际严格客户端不兼容，不能继续沿用。

## 修复边界

- 精确识别结构化 `request_timeout`，仅未提交输出且客户端仍在线时进入现有有界故障转移。没有扩大全局重试次数或默认原账号重试状态码。
- OpenAI Responses 的 APIKey 与 OAuth 统一使用已有裸错误终态处理。保留原错误码；有真实 `response.failed` 时采用真实终态，只生成一次失败；后续真实 completed/done 优先，`[DONE]` 不抹掉失败。没有伪造成功或用量。
- 最终失败复用既有 Ops 登记函数，保留最后账号/request ID/原始详情；关闭正文记录时清除旧详情。裸 error 与 failed 不重复记录，重复 failed 仅继续收集用量。
- 客户端取消后，不将读取错误误判为可重放失败，已知用量和缓存用量继续返回。成功恢复时清除旧错误的免计费标记，不修改价格、余额、预扣或数据库。
- Responses 事件序号 0 必须序列化。合成失败保留可获得的原序号，独立网关失败无原序号时使用 0；compact 输出项和 completed 连续编号。补齐合成失败 response 的 created_at。
- 复核发现并修正透传失败合成前的 keepalive 停止顺序，确保正式写出前结束并发心跳。

## 验证记录

- 超时回归先失败，修复后通过，覆盖 APIKey/OAuth、原生/透传、输出前后、裸错误接 DONE、真实失败、成功恢复、缓存用量及旧 attempt 污染。
- 零序号回归先失败，修复后通过。覆盖 created/in_progress/delta/completed/failed/error，以及 handler、compact、HTTP/WS 合成事件。
- 相关三个包完整 unit 测试通过：apicompat 0.741 秒、service 211.228 秒、handler 139.814 秒。随后新增取消后非正常 EOF 用量回归，纳入最终定向验证。
- 独立审查发现取消读取错误、成功恢复残留免计费标记及重复失败登记三处边界，均已修复并补回归。
- 三包定向 race 通过：service 12.373 秒、handler 4.057 秒、apicompat 1.632 秒。最终又补取消后空 completed/done 不登记 failover，以及 response.done 恢复测试。
- 两机 Linux 临时目录运行 24 项守护测试和 1 项交接失败恢复测试均通过，全部模拟操作，没有改动真实 nginx 或应用。
- 322 真实 Grok Responses 基线：469 个事件，首字 31.179 秒，总 36.151 秒，371 正文片段，缓存 640，正常 completed，缺序号和非法序号均为 0。这只能证明该请求路径正常，不能替代转换/失败分支回归。
- 源码 CI 35054366356、标签 CI 35055010334、源码安全扫描 35054366509、标签安全扫描 35055010330 全部成功。最初 CI 35053530598 的唯一失败是新测试类型断言未检查；已由 224d57776 修正，未绕过检查。
- 最终补充取消/done 定向 race 5.043 秒通过。官方 openai-python 的 ResponseFailedEvent 定义确认 sequence_number 为必需 int；部分文档旧示例缺字段，不能把示例当作可省略的证据。

## 部署要求

- 候选 323 使用 8091/pprof 6070，原 322 的 8090 保持运行，旧连接自然结束。
- 复用已修正的探针规则和事务交接：1% 至少观察 180 秒且至少 50 次成功业务请求，之后才允许全量；全量至少监控 300 秒。
- 仅平台健康、应用存活和内存余量触发回退。上游业务 502/503 不作为自动回退依据，499 探针取消不视为平台失效。
- 新旧物理服务器份额保持旧 40%、新 60%。生产环境、nginx 配置先备份，数据库仅校验迁移。

## 后续排障经验

- 客户端的反序列化错误必须验证真实 JSON 字段存在，不能只把缺字段解码成 Go 零值后宣称正常。
- 顶层 `code` 与嵌套 `error.code` 都可能来自合法上游错误。错误分类应优先结构化证据，不能凭文案里的 timeout 开启重试。
- bare error 可能只是前导事件，最终 completed/failed 才权威。延迟副作用与错误登记能避免成功请求被标失败、重复扣减健康值或误免单。
- 完成事件修补不会让真实上游断流消失。已输出内容不能透明重放；仍需按真实最后 attempt 排查上游稳定性。

## 发布与灰度实测

- v0.1.323 发布于 UTC 04:31:33，Release 35055010498 成功。标签源码 224d57776de96d44e10a86ab206a4eca35d00606；main VERSION 自动同步提交 e73a3b887。
- Linux amd64 压缩包 SHA256：86236cc94a9aa09d17f8ca56e2b1c572a6884e2cd42e13ecf42dfe2a701034eb；二进制：df0fd917211d32be782d033cc4162739bf5859c03df30bc932ad506094911639。两机核验一致。
- 新机 323 PID 2086159，旧机 PID 779457；322 PID 2056026/261660 原样保留。候选 8091、pprof 6070，迁移仅 validate。
- 安装备份：新机 /root/sub2api-install323-20260916T043256Z，旧机 /root/sub2api-install323-20260916T043305Z。守护目录 /opt/sub2api/rollout-323-20260916，脚本 SHA256 135816d2e67e0af53f080b425c54778b887c3ee974c6e8a776c337d77891f299。
- 北京时间 12:34:23/12:34:29 两机分别进入 1%。原守护停止后等待任务结束并持锁交接，原应用未停止。12:38:45/12:38:50 晋级 100%，灰度超过 4 分钟，每个入口至少 50 成功业务请求。
- 真实复现：322 的 deepseek-v4.1-flash Chat→Responses 请求在 369 个事件中缺 response.created 的序号（请求 d41d4543-a082-4bd9-8f7c-029ba1105167，账号 29159）。两机 323 同路径分别 385/405 个事件全部有合法序号、正常 completed。
- 公网同路径 412 个事件，首字 1.968 秒、总 4.689 秒、405 正文片段，缺序号/非法序号都为 0，请求 bf53f49d-e21e-4acc-9282-bd8b24ac23b8。
- 灰度 GPT：首字 1.496 秒、总 6.821 秒、288 正文片段、缓存 4224，账单一致。Grok Chat：31.356/36.425 秒、309 正文片段、缓存 128，账单一致。
- 公网 GPT：首字 2.024 秒、总 10.727 秒、285 正文片段、缓存 6784，账单一致；请求 40fa7100-83af-4372-b5b7-479735851f18。公网 Grok：40.728/45.340 秒、352 正文片段、缓存 2176，账单一致；请求 2106cb49-ae17-466f-b5e8-7527d747e43d。Grok 仍有真实上游等待，不把分段正常说成消除了慢首字。
- 最小输入策略三协议真实错误 76360804/805/806 均保留 400/400 和原始详情，error_message 带精确 minimum input 标记，继续排除渠道健康计错；未改统计表或重放历史修正事务。
- 全量初期剩余平台 5xx 主要为 gpt-5.5 路由无可用账号，发布前相同模型已有大量同类记录。Gemini 429/404、Claude 上游 502 也仍存在；无新增平台崩溃证据，不因上游业务错误回退。
- 北京时间 12:44:40/12:44:46 完成全量验收，分别观察约 355/356 秒，34/31 次成功守护检查；phase=verified、percent=100、armed=true，公网版本均为 0.1.323。最初验收不足 300 秒被门槛拒绝，等待足时后通过，没有修改门槛。
- 12:44:51/12:44:56 复核：两入口 323 健康探针 4561/4546 次均 200，应用及原 322 PID 不变、NRestarts=0，无 panic/OOM/数据库槽位耗尽。CPU 约 8.49%/24.06%，整机可用内存约 41.6/32.9 GiB；未调整物理 40/60 分流。
- 同期 323 RSS 约 3.81/1.97 GiB；闲置 322 RSS 已自然回落到约 1.60/0.52 GiB。短时观测不能证明长期无泄漏，没有停止旧进程或强制 GC。
- 真实超时记录 76365458：最后账号 28659、detail 保留顶层 request_timeout 和原始断流消息，kind=stream_failed、owner=provider、phase=upstream。该记录 wire HTTP 为 200，Ops 文案含 Recovered，但没有匹配的成功 usage 证据，**不能据此宣称故障转移恢复成功**；本次证据只确认最终上游归因已修正。后续可单独核对取消后 HTTP200 的 recovered 文案。
- 321/更旧实例未在本轮清理；318 仍可能被 overlay 引用。原 322 保留，守护持续运行；不把上游业务错误当成版本回退理由。
