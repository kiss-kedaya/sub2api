# 0.1.349 发布与部署记录

普通用户可提交工单，管理员可处理。每人每个北京时间自然日只能新建一单，关闭不返还名额。联系方式和正文不进审计请求体。新增迁移 `245_support_tickets.sql`，只建工单表，不改余额、价格、倍率、支付或分组。`DATABASE_MIGRATION_MODE=validate`，`GATEWAY_GROK_RESPONSE_HEADER_TIMEOUT=600`，预扣保持关闭。

348 留着当备份，进程不关。347、345 仍在跑，但不在 nginx 里。

## 发布

- 功能提交 `628f3fb39`，版本提交 `d0eb8686b`。二进制 SHA256：`583c3795823cde65f886d023c15d95b1c3771a5e587fd384c6c4e2950865a2f4`。
- 前端入口：`/assets/index-D0Wwk-sq.js`、`/assets/index-DR6UDIe9.css`。
- 迁移前备份：`/opt/sub2api/release-backups/schema_migrations-pre-349-20260924.csv`。`schema_migrations` 已写入 245，校验和 `5a8dc2497c7e5db1809baea122f3fa51f90978e3063bfaa578252121b3f953aa`。

## 滚动上线

- 候选：349 / 内网 8140 / pprof 6102。稳定回退：348 / 8128。1% 期间备份仍是 347 / 8136。
- 切 1% 前已停 348 守卫。1% 大约 11 分钟，349 有真实 200，无平台 panic，NRestarts=0。
- 两台切到 349 全量。348 作为 nginx backup。新机静态入口在切满后换成新 `index.html`，回退会换回旧入口。

| 节点 | 业务流量比例 | 349 PID | 保留的 348 PID | 启动时 NRestarts |
| --- | --- | --- | --- | --- |
| ns575199 | 40% | 2117494 | 1337076 | 0 |
| ns572368 | 60% | 3003595 | 2936677 | 0 |

## 验收

- 两台 349 `/readyz` 为 ready。两台本机 Host 探活首页是 `index-D0Wwk-sq.js`，对应 JS/CSS 为 200。
- 未登录访问 `/api/v1/tickets`：349 返回 401，348 返回 404。
- 守护 `watching / percent=100 / armed=true`。纯上游 502/503/429 不回退。

回退入口：对应服务器执行 `python3 /opt/sub2api/rollout-349-20260924/rollout349_guard.py rollback`。新机命令前加 `sudo -n`。只切回 348 流量和静态入口，不停应用、不删旧资源。
